## Blocking findings

Public runtime inventory: 15 components — Button, Chip, Badge, Switch, Checkbox, Radio, Input, Textarea, Select, Tooltip, Toast, Table, Field, Icon, Typography — plus `renderCriticalCss` and `honoRendererScope`. The harness imports and instantiates every component, so there is no name-level omission.

1. **Icon visibly fails in the supplied harness.** The harness serves only `podo-ui/styles.css` ([server.tsx](</private/tmp/claude-501/-Users-tarucy-project-podoui/885735ab-d9d3-4bc8-9008-5a94bf821edb/scratchpad/harness/hono/src/server.tsx:28>)); the package documentation requires `podo-ui/icons.css` separately ([README.md](/Users/tarucy/project/podoui/packages/podo-ui/README.md:39)). Accordingly, `hono-shots/01-inventory.png` shows a completely blank Icon section and missing Button/Input/Toast/Field slot glyphs. The report’s class-count assertion proves only that empty `<i>` elements exist. Worse, the installed harness package predates the post-fix `./icons.css` export, while the post-fix report compares only `dist/hono` and `styles.css` (`hono.md:96-103`) and explicitly declines a browser rerun. The repacked default icon CSS also defines `menu` but not the tested `search` or `close` names ([PodoIcons.css](/Users/tarucy/project/podoui/packages/podo-ui/dist/icons-assets/PodoIcons.css:18)). This alone refutes “every component renders correctly.”

2. **Select cannot be given an accessible name or wired to its listbox.** `HonoSelectProps` exposes no `id`, `aria-label`, or `aria-labelledby` ([index.tsx](/Users/tarucy/project/podoui/packages/hono/src/index.tsx:192)); the combobox and listbox have no IDs or `aria-controls` relationship ([index.tsx](/Users/tarucy/project/podoui/packages/hono/src/index.tsx:295)). The browser snapshot confirms the form Select is unnamed: `combobox [expanded=false]` rather than a quoted accessible name (`hono-commands.log:34`). This contradicts the source-of-truth accessibility contract requiring `aria-controls` ([select.component.json](/Users/tarucy/project/podoui/packages/spec/samples/components/select.component.json:195)). The report checks only role/count attributes and misses the broken wiring.

3. **Select violates its read-only contract and has invalid addable composition.** Rendering `<Select readOnly open>` still emits `data-open`, `aria-expanded="true"`, and the listbox because `open` is never suppressed for read-only or disabled state ([index.tsx](/Users/tarucy/project/podoui/packages/hono/src/index.tsx:288), [index.tsx](/Users/tarucy/project/podoui/packages/hono/src/index.tsx:350)); the JSON contract says read-only “does not open” ([select.component.json](/Users/tarucy/project/podoui/packages/spec/samples/components/select.component.json:123)). With `addable`, an input and button are placed directly inside `role="listbox"` rather than outside it or under valid option/group structure ([index.tsx](/Users/tarucy/project/podoui/packages/hono/src/index.tsx:351)). The harness avoids `addable`, `readOnly+open`, and `disabled+open`.

4. **Indeterminate Checkbox is only painted as mixed, not exposed as mixed.** Core correctly computes `aria-checked="mixed"` ([core/index.ts](/Users/tarucy/project/podoui/packages/core/src/index.ts:266)), but the Hono component discards `behavior.root` and emits only `checked={false}` plus `data-state="indeterminate"` ([hono/index.tsx](/Users/tarucy/project/podoui/packages/hono/src/index.tsx:745)). Assistive technology therefore receives an unchecked checkbox, contradicting the explicit accessibility contract ([checkbox.component.json](/Users/tarucy/project/podoui/packages/spec/samples/components/checkbox.component.json:107)). The report merely counts the visual `data-state`.

5. **`Chip removable disabled` ignores `disabled`.** Although both props are public, the removable branch returns before applying the computed button behavior and renders an enabled removal button with no disabled/ARIA/data-disabled state ([index.tsx](/Users/tarucy/project/podoui/packages/hono/src/index.tsx:584)). Its disabled CSS also targets `.podo-chip[disabled]`, which the removable `<span>` never receives ([styles.css](/Users/tarucy/project/podoui/packages/podo-ui/styles.css:358)). The harness tests disabled and removable only as separate cases, concealing this combination bug.

6. **Field’s optional ID path produces duplicate IDs.** Every `<Field>` without an explicit ID defaults to the same `"podo-field"` seed ([index.tsx](/Users/tarucy/project/podoui/packages/hono/src/index.tsx:893)), producing duplicate `podo-field-control`, label, description, and error IDs. The harness supplies a unique ID for every Field and never exercises the advertised optional default. Additionally, `wireHonoControl` blindly overwrites a child’s existing `id` and ARIA attributes rather than preserving/joining them ([index.tsx](/Users/tarucy/project/podoui/packages/hono/src/index.tsx:1010)).

7. **The “raw command log” is not reproducible evidence.** Commands such as `agent-browser eval componentAssertions` and `eval componentAssertions2` (`hono-commands.log:9-16`) omit the actual JavaScript expressions and selectors. The resulting JSON counts therefore cannot be audited or reproduced. Several report claims cite “raw HTML” and `curl` output, but neither the commands nor that output appear in the supplied log. The log also contains an ignored evaluation failure, `SyntaxError: Identifier 'q' has already been declared` (`hono-commands.log:12`). Rendering `honoRendererScope` merely proves that a constant describes the intended contract; it is tautological evidence that the implementation satisfies it.

8. **Functional coverage is far below the breadth of the claim.** The only interactions are fill, checkbox check/click, radio click, and form submission (`hono-commands.log:38-66`). There are no keyboard commands at all, no disabled no-op checks, no read-only mutation attempts, no required/maxLength validation, no reset-button check, and no focus-order/a11y checks for the emitted close/remove/clear/add controls. Accepting the documented SSR-static limitation excuses absent client state transitions; it does not turn untested native keyboard, disabled, focus, and form behavior into proof of correctness.

9. **`renderCriticalCss` permits script-breakout through `theme`.** `theme` is an unrestricted string and is JSON-stringified into raw script text ([index.tsx](/Users/tarucy/project/podoui/packages/hono/src/index.tsx:984)). A value containing `</script><script>…` terminates the script element at the HTML parser level despite being inside a JavaScript string. The harness tests only the safe literal `"dashboard"` and provides no escaping/security case.

## Non-blocking suggestions

- Reinstall the final tarball into a clean harness, load the required icon asset, and rerun every browser check.
- Preserve exact executable evaluation expressions and add assertions that fail the command instead of returning descriptive counts.
- Add combination tests for Select state conflicts, removable-disabled Chip, default-ID Fields, and indeterminate accessibility.

VERDICT: FAIL
