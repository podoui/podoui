# Podo UI v2 — Hono SSR runtime: FROZEN-ARTIFACT FINAL verification (session `hono8`)

**This report supersedes all previous hono rounds (hono1–hono6, including the previous "definitive" hono5 and the R5 spot-check hono6).** Every claim below cites output produced in THIS session (`hono8`) against the frozen tarball; everything older lives in the History appendix and is not stated as current fact.

| | |
|---|---|
| Artifact | **FROZEN**: `podo-ui-2.0.4.tgz`, sha1 `634f24adaa889b613dbc1fee9e8f77c72ddf3d3e` — verified as this session's first log entry (banner + shasum, log `:1631-1634`) and re-hashed unchanged at teardown (log `:2091-2094`) |
| Session | `hono8` — every browser step ran as `npx agent-browser --session hono8 …` via the `abh8.sh` wrapper, which echoes the FULL shell-quoted command line (entire eval JS) into the log before running |
| Date | 2026-07-21, ~12:14–12:35 KST |
| Runtime | hono ^4.12 + @hono/node-server, JSX via `hono/jsx`, tsx on node v25.8.2, server `127.0.0.1:5213` |
| Freshness | Clean reinstall `rm -rf node_modules/podo-ui package-lock.json && npm install ../../podo-ui-2.0.4.tgz` (log `:1636-1640`). Bit-for-bit: installed `styles.css` sha1 **`fa65c26a969aa0b521d9b6c2159a02d0c27a05b7`** == `tar -xOzf …tgz package/styles.css \| shasum` (log `:1646-1648`) — **CHANGED this round** vs hono6's `55d4f4e3…` (the ported v1 DatePicker/Editor CSS landed; browser-parsed rule count moved 768→779, item 1). Installed `dist/hono/index.js` sha1 `94d873ccb651de71ed01047b4981e313aa86409d` == tarball copy (log `:1642-1644`; byte-identical to hono6 — the last two commits touched native/react/icons, not the hono renderer). Also hash-matched: `dist/icons-assets/PodoIcons.css` `10420f0d…` and `PodoIcons.woff2` `be01caff…` vs tarball (log `:1650-1654`). Installed identity `{"name":"podo-ui","version":"2.0.4"}`; lockfile resolves `file:../../podo-ui-2.0.4.tgz` (log `:1656-1660`). Served `/styles.css` == installed copy (`fa65c26a…`, log `:1687-1690`) |
| Repo cross-check | `/Users/tarucy/project/podoui` `pnpm check` green: `Test Files  23 passed (23)` / `Tests  326 passed \| 2 skipped (328)` — `check-final.log:73-74`. Repo READ-ONLY this session (sources consulted only for expected semantics; the only builds run were inside the scratchpad harness) |
| Rebuild | `podo build --force` executed from the installed tarball's own CLI (`dist/cli/index.js init` + `build --force`, log `:1662-1665`) to produce the real generated `components.css` for item 16 (B2); harness has no other build step (tsx runs `src/server.tsx` directly against the tarball's export map, `/resolved-paths` log `:1677-1678`) |
| Log | `reports/hono-commands.log`, hono8 banner at line **1631** (`=== FROZEN FINAL VERIFICATION session hono8 tarball 634f24ad… ===`); every command — including the FULL JS of every `eval` — is echoed into the log before its output (runners `run8.sh` for shell, `abh8.sh` for agent-browser; `abh8.sh` passes args straight through with no `sh -c` reinterpretation) |
| Raw bodies | `reports/hono8-raw-{inventory,form,form-required,xss,xss-css,badge-vars}.html` (curl, this session, log `:1679-1686`; badge-vars re-curled after the harness style fix, log `:1836-1840`); per-section raw SSR quotes at log `:1746-1827` |
| Screenshots | `reports/hono-shots/m01…m10` (m-prefix = this session; j-/h-/g-/f-/0-prefix files are history) |

## How to reproduce

```sh
SP=/private/tmp/claude-501/-Users-tarucy-project-podoui/885735ab-d9d3-4bc8-9008-5a94bf821edb/scratchpad
shasum "$SP/podo-ui-2.0.4.tgz"          # 634f24adaa889b613dbc1fee9e8f77c72ddf3d3e
cd "$SP/harness/hono"
rm -rf node_modules/podo-ui package-lock.json && npm install ../../podo-ui-2.0.4.tgz
node node_modules/podo-ui/dist/cli/index.js build --force   # regenerates src/podo/components.css (item 16)
sh start.sh    # serves 127.0.0.1:5213: / /form /form-required POST /echo /xss /xss-css /badge-vars
               #   /styles.css /icons.css (+/PodoIcons.woff{,2}) /components.css /resolved-paths
"$SP/abh8.sh" open http://127.0.0.1:5213/    # etc; wrapper logs full command + output
kill "$(cat server.pid)"                 # teardown
```

## Verdict

**20/20 checklist items PASS on clean runs in session hono8** (full re-run of the established 14 + the 6 new B-items). Zero console messages and zero page errors on every route; the only script element on any page is the `renderCriticalCss` theme stamp. Three tool/harness-side artifacts occurred and each affected check was cleanly rerun — see Environmental artifacts. All log pointers below are line numbers in `reports/hono-commands.log` (hono8 section starts at `:1631`).

## Checklist results — established 14 (full re-run)

| # | What was exercised | Result | Evidence (quoted from this session's output) |
|---|---|---|---|
| 1 | Glyphs: `/styles.css` + `/icons.css` resolved via the package export map (`/resolved-paths` → `…/node_modules/podo-ui/styles.css`, `…/dist/icons-assets/PodoIcons.css`, log `:1677-1678`), parsed by the browser; computed styles of all 9 glyphs after `document.fonts.ready` — now WITHOUT any harness font-size override (see item 15) | PASS | `"fontsLoaded":"loaded","count":9`; every glyph `"fontFamily":"PodoIcons"`, `"width":24` (from the component's own `data-size="md"` CSS), one PUA codepoint each: `[{"icon":"menu",…,"codepoints":["U+e001"]},…,{"icon":"search",…,"codepoints":["U+e009"]}]` (log `:1714-1715`). Stylesheets parsed: `{"href":"http://127.0.0.1:5213/styles.css","rules":779}` (768→779 vs hono5 — the new DatePicker/Editor CSS of this build), `{"href":"http://127.0.0.1:5213/icons.css","rules":11}` (log `:1716-1717`). Screenshot `m01-inventory.png` |
| 2 | Inventory rendering all 15 components; renderCriticalCss wiring; honoRendererScope | PASS | Non-zero root counts for all 15: `"counts":{"button":7,"chip":13,"badge":4,"switch":6,"checkbox":6,"radio":5,"input":14,"textarea":6,"select":11,"tooltip":4,"toast":7,"table":2,"field":15,"icon":23,"typography":4}`; `"themeDataset":"dashboard","colorScheme":"light","criticalVar":"\"loaded\"","criticalStyleTagPresent":true,"themeScriptCount":1`; scope string exact: `"rendererScope":"Hono components render static SSR HTML. Interactive behavior is emitted as attributes and delegated to a future client entry."` (log `:1718-1719`). Screenshot `m01-inventory.png` |
| 3 | Select wiring: open single, open multi (chips + `+1` overflow + clearable), addable — with the add row OUTSIDE (before) the listbox | PASS | Raw SSR (log `:1746-1827`): `<div id="fruit" … role="combobox" aria-haspopup="listbox" aria-expanded="true" aria-controls="fruit-listbox" aria-label="과일 선택" tabindex="0">` + `<div id="fruit-listbox" … role="listbox">` with `role="option" aria-selected="true"`; multi: `<div id="multi-fruit-listbox" … role="listbox" aria-multiselectable="true">`, trigger shows 2 removable chips + `<span class="podo-select__chip-more" aria-label="외 1개 선택됨">+1</span>` + `<button … class="podo-select__clear" aria-label="모두 해제">`; addable: `<div class="podo-select__add"><input class="podo-select__add-input" placeholder="과일 이름 입력"/><button type="button" class="podo-select__add-button">추가</button></div>` rendered BEFORE `<div id="addable-fruit-listbox" … role="listbox" …>` — positional proof `{"addRowIndex":1025,"listboxIndex":1236,"addRowBeforeListbox":true,"addInput":true,"addBtnText":"추가"}` (log `:1731-1732`) |
| 4 | Select closed states: closed carries NO aria-controls; invalid / readOnly / disabled ARIA; readOnly+`open` and disabled+`open` render CLOSED; disabled multi chips static | PASS | Programmatic audit of all 11 triggers (log `:1729-1730`): `veg` closed → `"expanded":"false","controls":null`; invalid → `"invalid":"true"`; `ro-select` → `"readonly":"true","tabindex":"0"`; `dis-select` → `"disabled":"true","tabindex":"-1"`. `open` prop suppressed: `"roOpenProp":{"listboxes":0,"dataOpen":null},"disOpenProp":{"listboxes":0,"dataOpen":null}` (log `:1731-1732`). Disabled multi renders 2 static chips `{"state":"selected","removable":"true","disabled":"true","removeBtns":0,"tag":"SPAN"}` ×2 — no remove buttons (log `:1731-1732`; raw markup confirms `<span class="podo-chip" … data-disabled="true"><span class="podo-chip__label">Apple</span></span>` with no `.podo-chip__remove`, log `:1746-1827`) |
| 5 | Named comboboxes: EVERY rendered select trigger carries an accessible name | PASS | `"total":11,"allNamed":true` — quoted names: `단일 과일 선택`, `과일 선택`, `채소 선택`, `다중 과일 선택`, `과일 추가 선택`, `읽기 전용 선택`, `비활성 선택`, `비활성 다중 선택`, `유효하지 않은 선택` (all via aria-label) and the two Field-wrapped triggers via `aria-labelledby` resolving to `"과일*"` / `"Invalid propagation (select)"` (log `:1729-1730`). The `/form` select is the tab-8 combobox with `aria-label="Static fruit select"` (raw form body `hono8-raw-form.html`) |
| 6 | Checkbox: indeterminate ARIA + `:checked` repaint BOTH directions via real `.click()` toggles measuring computed backgroundColor | PASS | `<input class="podo-checkbox" type="checkbox" aria-checked="mixed" data-state="indeterminate">` (live outerHTML, log `:1724-1725`; raw SSR log `:1746-1827`). Repaint: `"unchecked_before":{"checked":false,"bg":"rgb(255, 255, 255)"}` → `"unchecked_afterToggle":{"checked":true,"bg":"rgb(66, 108, 237)"}` and `"ssrChecked_before":{"checked":true,"bg":"rgb(66, 108, 237)"}` → `"ssrChecked_afterUntoggle":{"checked":false,"bg":"rgb(255, 255, 255)"}`; `"restored":{"un":false,"ch":true}` (log `:1724-1725`). CSS cause in the installed file: `.podo-checkbox:checked` at `styles.css:1017` (log `:1726-1728`) |
| 7 | Field ids: label-derived (English + Korean), explicit child id preserved with JOINED describedby, disabled Field | PASS | `field-auto-en`: `"for":"podo-field-company-control","id":"podo-field-company-control","forMatchesId":true`; Korean: `"for":"podo-field-이름-control","id":"podo-field-이름-control"` with `"describedby":"podo-field-이름-description","describedbyResolves":true`; explicit id: `"for":"my-input","id":"my-input","describedby":"external-hint podo-field-website-description"` — joined, not clobbered; disabled: `"id":"locked-field-control","disabled":true,"value":"frozen"` (log `:1733-1734`). Raw SSR quotes for all four at log `:1746-1827` |
| 8 | Field required / invalid propagation / disabled-OR (+ `<form><Field required>` native `required` on bare Checkbox/Radio with blocked empty submit) | PASS | Invalid WITHOUT child prop — Input: `{"rootDataState":"invalid","inputAriaInvalid":"true","inputDataInvalid":"true"}`; Select: `{"rootDataState":"invalid","triggerAriaInvalid":"true"}`; disabled-OR (`<Field disabled><Input disabled={false}/></Field>`): `{"hasDisabledAttr":true,"disabledProp":true,"value":"still frozen","wrapperState":"disabled"}` (log `:1735-1736`; raw: `<input id="fd-override-control" … value="still frozen" disabled="" …/>`, log `:1746-1827`). `/form-required`: `termsHasRequired:true`, `planHasRequired:true` (both also `aria-required="true"`), `formCheckValidity:false`, messages `"계속하려면 이 확인란을 선택하세요."` / `"다음 옵션 중 하나를 선택하세요."`; empty submit clicked → **blocked**, `{"hrefAfterEmptySubmit":"http://127.0.0.1:5213/form-required","stillOnForm":true}` (log `:2041-2046`, screenshot `m10-required-blocked.png`); after checking both via real Space presses the same submit succeeds → `/echo` exactly `{"terms":"accepted","plan":"pro"}` (log `:2049-2064`) |
| 9 | Chip removable+disabled markup | PASS | `<span class="podo-chip" data-theme="solid" data-size="md" data-state="selected" data-removable="true" data-disabled="true">…<button type="button" class="podo-chip__remove" aria-label="필터 제거" disabled="" aria-disabled="true">` — `"btnDisabled":true,"btnAriaDisabled":"true"` (live outerHTML log `:1733-1734`; raw SSR log `:1746-1827`) |
| 10 | renderCriticalCss BOTH escapes — theme (`</script><script>alert(1)//`) and css (`</style><script>alert(1)//`) — raw curl bytes + browser (same evidence backs item 19/B5) | PASS | `/xss` raw bytes (log `:1691-1692`): `dataset.podoTheme="</script><script>alert(1)//"` — literal `</script` count in the whole page = **1** (the element's own close tag); `u003c/script` and `u003cscript` both present (log `:1693-1696`). `/xss-css` raw bytes (log `:1698-1699`): `<style data-podo-critical="true"><\/style><script>alert(1)//</style>` — payload's `</style` neutralized to `<\/style`; literal `</style` count = **1**; the `<script>alert(1)//` bytes sit inside style raw text (log `:1700-1702`). Browser: `/xss` → `{"podoTheme":"</script><script>alert(1)//","scriptCount":1,"bodyText":"escape probe"}` — payload inert as data, no dialog (log `:1856-1858`); `/xss-css` → `{"scriptElementCount":1,"onlyScriptIsThemeStamp":true,"styleTextContent":"<\\/style><script>alert(1)//","podoTheme":"dashboard"}` (log `:1867-1869`). Errors/console `[EMPTY]` after both (log `:1859-1861`, `:1870-1872`). Screenshots `m04-xss-theme-probe.png`, `m05-xss-css-probe.png` |
| 11 | Form e2e: fill/check/select/submit → `/echo` JSON | PASS | Pre-submit: `{"email":"hono8@podo.dev","bio":"SSR bio from session hono8","code":"12345","agree":true,"news":true,"colorChecked":"red","ro":"locked-value","disDisabled":true}` (log `:1974-1975`); Submit → `"href":"http://127.0.0.1:5213/echo"` with body exactly `{"email":"hono8@podo.dev","bio":"SSR bio from session hono8","code":"12345","ro":"locked-value","agree":"yes","news":"daily","color":"red"}` — readOnly `ro` submits; disabled `dis` ABSENT (log `:1978-1981`). Screenshots `m07-form-filled.png`, `m08-echo-response.png` |
| 12 | Keyboard: Tab order across the form (disabled skipped); Enter submits from a text input | PASS | From BODY (log `:1882-1883`), 12 real Tab presses, one eval per press (log `:1884-1931`): `f-email-control` → `f-bio-control` → `f-code-control` → `f-ro-control` (**readOnly IS focusable**, `"readOnly":true`) → **`f-dis-control` SKIPPED** → checkbox `agree` → checkbox `news` → radio `blue` (checked radio = group tab stop) → `DIV id="form-fruit" role="combobox"` → `BUTTON role="switch"` → `BUTTON type=submit "Submit"` → **disabled submit SKIPPED** → `BUTTON type=reset "Reset"` → `BODY` (wrap). Enter-in-input: fill + focus `#f-email-control` + `press Enter` → `{"afterEnterInInput":{"href":"http://127.0.0.1:5213/echo","body":"{\"email\":\"enter-submit@podo.dev\",…}"}}` (log `:2027-2037`) |
| 13 | Native validation: required-empty blocks submit; maxlength clips real keystrokes; reset restores SSR defaults | PASS | Blocked: clicked enabled Submit with empty required email → `{"stillOn":"/form","valueMissing":true,"validationMessage":"이 입력란을 작성하세요.","formInvalid":true}` (log `:1952-1957`), screenshot `m06-required-blocked.png`. Clip: `keyboard type "1234567890"` into `maxLength=5` → `{"typed":"1234567890","codeValue":"12345","length":5,"maxLength":5}` (log `:1960-1965`). Reset: after filling everything (incl. unchecking default-checked `news` and selecting `red`), click `type=reset` → `{"afterReset":{"email":"","bio":"","code":"","agree":false,"news":true,"colorChecked":"blue","ro":"locked-value"}}` — SSR defaults restored (log `:1984-2002`), screenshot `m09-after-reset.png` |
| 14 | Toast/Tooltip/Badge/Table/Typography variant spot-checks | PASS | Toasts (7): `danger` is `"role":"alert","closable":true`, all others `"role":"status"`; info `"caption":"Extra caption line"`; warning `"suffixText":"실행 취소"`; closable+custom-suffixIcon toast renders the custom icon INSIDE the close button (raw: `<button class="podo-toast__close" type="button" aria-label="닫기"><i class="podo-icon podo-icon-refresh" …></i></button>`, no default X path; suffix-only renders `<span class="podo-toast__suffix-icon">`, log `:1746-1827`). Tooltips (4): positions right/top/bottom/left incl. `{"position":"top","theme":"reverse","ordinal":"second"}`, `"ordinal":"third"`, all `"hasArrow":true`. Badges (4): natural/danger/success-dot(`"ariaLabel":"online"`)/purple. Tables: `{"type":"grid","rows":2,"headerCells":2}`, `{"type":"horizon","rows":1,"headerCells":0}`. Typography: `P podo-text podo-text--body`, `SPAN podo-text podo-text--body`, `H1 podo-text podo-text--h1` (log `:1722-1723`). Visible in `m01-inventory.png` |

## Checklist results — new items (B1–B6)

| # | What was exercised | Result | Evidence (quoted from this session's output) |
|---|---|---|---|
| 15 (B1) | Icon `size` renders COMPUTED 16/24/32px with the harness font-size override REMOVED (the forced `.icon-row i { font-size: 24px }` the reviewer caught is gone — the live page's only `.icon-row` rule is quoted from the browser's CSSOM); `decorative={false}` → `role="img"` + label | PASS | Computed, from the component's own `data-size` CSS: `"sm":{"dataSize":"sm","computedFontSize":"16px","rectWidth":16,"rectHeight":16}`, `"md":{…"24px",…24,…24}`, `"lg":{…"32px",…32,…32}`; harness rule now `"harnessIconRowRule":".icon-row i { color: rgb(39, 39, 42); }"` — NO font-size (log `:1720-1721`). DOM + computed for the informative icon: `"outerHTML":"<i class=\"podo-icon podo-icon-search\" data-size=\"md\" role=\"img\" aria-label=\"검색\"></i>","role":"img","ariaLabel":"검색","ariaHidden":null,"computedFontSize":"24px"`; decorative default: `{"role":null,"ariaHidden":"true"}` (log `:1720-1721`). Corresponding CSS in the installed styles.css: `.podo-icon[data-size="sm"]{font-size:16px}` / md 24px / lg 32px. Screenshot `m02-icon-spec.png` |
| 16 (B2) | Badge binding vars: fallback path (NO `--podo-badge-*` defined) must match the Figma hexes; binding path (vars defined) must follow the vars. The REAL `podo build --force` components.css of THIS tarball's CLI is served at `/components.css` (sha1 `c8141d1f…` served == generated file, log `:1687-1690`); since the default build emits NO badge vars (see Observations), the binding definitions are injected as an equivalent `<style>` on `/badge-vars` | PASS | **Fallback** (section without vars): `natural {"bg":"rgb(62, 66, 75)","color":"rgb(249, 249, 249)"}` = `#3e424b`/`#f9f9f9`; `danger {"bg":"rgb(242, 59, 59)"}` = `#f23b3b`; `purple {"bg":"rgb(248, 245, 255)","color":"rgb(142, 81, 255)"}` = `#f8f5ff`/`#8e51ff`; `green dot {"bg":"rgb(62, 168, 86)"}` = `#3ea856` — ALL equal the Figma fallback hexes baked into styles.css. **Bound** (vars injected: root-bg `rgb(10, 20, 30)`, label `rgb(200, 210, 220)`, dot `rgb(1, 2, 3)`): natural/danger/purple all `{"bg":"rgb(10, 20, 30)","color":"rgb(200, 210, 220)"}` and green dot `{"bg":"rgb(1, 2, 3)"}` — colors follow the binding vars exactly; the injected rule as parsed by the browser is quoted: `[data-testid=\"badge-bound\"] { --podo-badge-root-background: rgb(10, 20, 30); --podo-badge-label-color: rgb(200, 210, 220); --podo-badge-dot-color: rgb(1, 2, 3); }` (log `:1845-1846`). `/components.css` parses (3 rules) and `"componentsCssHasBadgeVars":false` (same eval). Raw body `hono8-raw-badge-vars.html`; screenshot `m03-badge-vars.png` |
| 17 (B3) | Keyboard contracts on real controls with REAL key presses: focused Checkbox + Space toggles; Radio group Arrow keys move+select and Space selects a focused unchecked radio; focused Button + Enter AND Space both activate (form effect) | PASS | **Checkbox** `agree`: `{"focused":true,"checkedBefore":false}` → `press Space` → `{"afterSpace1":true,"active":"agree"}` → `press Space` → `{"afterSpace2":false}` (log `:1932-1941`). **Radio arrows** (`color` group, blue checked): focus blue → `press ArrowLeft` → `{"afterArrowLeft":{"checkedValue":"red","activeValue":"red","activeName":"color"}}` → `press ArrowRight` → `{"afterArrowRight":{"checkedValue":"blue","activeValue":"blue"}}` (log `:1942-1951`). **Radio Space** (unchecked required radio on `/form-required`): `{"focused":true,"radioCheckedBefore":false}` → `press Space` → `{"radioCheckedAfterSpace":true}` (log `:2049-2054`); required checkbox likewise `{"checkboxCheckedBefore":false}` → Space → `{"checkboxCheckedAfterSpace":true,"formNowValid":true}` (log `:2055-2060`). **Button Enter**: focus enabled `BUTTON type=submit` (`{"focusedTag":"BUTTON","focusedType":"submit","href":"/form"}`) → `press Enter` → `{"afterEnterOnButton":{"href":"http://127.0.0.1:5213/echo","body":"{\"email\":\"button-enter@podo.dev\",…}"}}` (log `:2005-2015`). **Button Space**: fresh `/form`, focus submit → `press Space` → `{"afterSpaceOnButton":{"href":"http://127.0.0.1:5213/echo","body":"{\"email\":\"button-space@podo.dev\",…}"}}` (log `:2016-2026`) — distinct email values prove each activation independently |
| 18 (B4) | Field-wrapped Switch/Checkbox/Select recheck on THIS build: label `for=` matches each control's actual id + aria set on the control element | PASS | All slots `"forMatchesId":true` with every aria reference resolving (log `:1735-1736`): **Checkbox** `{"for":"fs-agree-control","id":"fs-agree-control","labelledby":"fs-agree-label","describedby":"fs-agree-error","invalid":"true","required":"true"}`; **Switch** `{"for":"fs-alarm-control","id":"fs-alarm-control","tag":"BUTTON","role":"switch","labelledby":"fs-alarm-label","describedby":"fs-alarm-description","required":"true"}`; **Select** `{"for":"fs-fruit-control","id":"fs-fruit-control","tag":"DIV","role":"combobox","labelledby":"fs-fruit-label","describedby":"fs-fruit-error","invalid":"true","required":"true"}`; **Radio** `{"for":"fs-plan-control","id":"fs-plan-control","invalid":"true","required":"true"}`; **Button** `{"for":"fs-action-control","id":"fs-action-control","describedby":"fs-action-description"}` (by design the hono Button's Field-slot surface is id+describedby only; it is natively labeled by `label[for]` — button is a labelable element). Raw SSR quotes for all five at log `:1746-1827`, e.g. `<input id="fs-agree-control" class="podo-checkbox" type="checkbox" required="" aria-labelledby="fs-agree-label" aria-describedby="fs-agree-error" aria-invalid="true" aria-required="true" data-state="unchecked"/>` and `<button id="fs-alarm-control" class="podo-switch" type="button" role="switch" aria-checked="false" aria-labelledby="fs-alarm-label" aria-describedby="fs-alarm-description" aria-required="true" …>` |
| 19 (B5) | `renderCriticalCss` BOTH escapes (theme + css payloads) on this build | PASS | Same-session evidence as item 10 above: raw bytes log `:1692-1703`, browser log `:1853-1874`, screenshots `m04`/`m05`. Both payloads neutralized; only script element on either page is the theme stamp; errors/console `[EMPTY]` after both |
| 20 (B6) | Final console sweep | PASS | Script census on `/` and `/form`: `{"scripts":[{"src":null,"dataPodoTheme":true}]}` — the theme stamp is the ONLY script (log `:2065-2079`). `FINAL SWEEP [EMPTY] no page errors after /` + `…no console messages after /` (log `:2071-2073`), same for `/form` (log `:2080-2082`) and `/badge-vars` (log `:2087-2089`); earlier per-route sweeps: `/` (`:1711-1713`), `/badge-vars` (`:1850-1852`), `/xss` (`:1859-1861`), `/xss-css` (`:1870-1872`), `/form` (`:1879-1881`). No dialogs at any point in the session |

## Console / page errors

Zero, throughout the session. Checked with visible `[EMPTY]` markers after every route: `/` (log `:1711-1713`), `/badge-vars` (`:1850-1852`), `/xss` (`:1859-1861`), `/xss-css` (`:1870-1872`), `/form` (`:1879-1881`), plus the explicit FINAL SWEEP over `/`, `/form`, `/badge-vars` (log `:2065-2089`). The only script element on any page is the `renderCriticalCss` theme stamp (log `:2068-2069`, `:2077-2078`). No dialogs appeared at any point.

## Observations (non-blocking)

- The default `podo build` `components.css` generated by THIS tarball's CLI defines binding vars only for **button, field, and input** (`--podo-button-*`, `--podo-field-*`, `--podo-input-*`; full file is 3 rules, `grep -c podo-badge` = 0 with the full generated file quoted at log `:2102-2122`; build run log `:1662-1665`). The badge comment in `styles.css` ("emitted per-theme by `podo build`'s components.css") therefore describes the projected contract, not the default build output — badge var definitions must come from a project's own component spec bindings. The CSS contract itself (consume `--podo-badge-*` with Figma-hex fallback) is fully proven by item 16 in both directions. Not a runtime defect; flagged for the docs/spec backlog.
- `podo init`/`build` run from an empty subdirectory walks up to the nearest `package.json` to pick its root — it initialized the harness folder itself (`.podo/`, `src/podo/`) rather than the empty `podo-app/` cwd (log `:1662-1666`). Scratchpad-only; the repo was untouched.

## Environmental artifacts (tool/harness-side, not the app)

1. **Harness template-literal syntax error (first server start).** The hono8 harness edit that removed the icon font-size override initially used backticks inside the `criticalCss` JS template literal (in a CSS comment), so tsx/esbuild failed to parse `server.tsx` and the first `start.sh` run reported `server did not come up` (log `:1667-1669`). Fixed by removing the backticks; the next start came up cleanly (log `:1671-1675`). No app code involved.
2. **hono/jsx escapes quotes inside `<style>` text children (item 16, first attempt).** The injected badge-binding rule was emitted as `[data-testid=&quot;badge-bound&quot;]` — invalid CSS, 0 parsed rules — so the first `/badge-vars` eval showed the bound section identical to fallback (log `:1828-1832`; raw body confirmed the `&quot;` bytes). Harness fixed with an unquoted attribute selector (`[data-testid=badge-bound]`), server restarted, raw body re-curled (log `:1836-1840`), and the WHOLE check rerun cleanly (log `:1842-1847`); only the clean rerun backs the PASS. (Noteworthy but benign: this is hono/jsx's escaping being conservative in raw-text elements; `renderCriticalCss` itself emits its style/script content via the raw path and is unaffected — proven by items 10/19.)
3. **Screenshot flag misuse (m01, first attempt).** `screenshot <path> --full-page` put the flag in the path position and saved to a temp file (log `:1737-1738`); retaken correctly as `screenshot <path> --full` (log `:1743-1744`). No page state involved.
4. **No tab resets, about:blank hiccups, or shortcut interference occurred.** Keyboard use was `press Tab/Space/Enter/ArrowLeft/ArrowRight` and `keyboard type`; no check needed a reload for tab-hiccup reasons.

## Harness changes made this session (scratchpad only; repo untouched)

- `harness/hono/src/server.tsx`:
  - B1 fix: removed the `font-size: 24px` override from `.icon-row i` in the harness critical CSS (now color-only) so computed icon sizes come from the component's `data-size` CSS alone.
  - B2: new `/components.css` route serving the real `podo build --force` output (`src/podo/components.css`), and new `/badge-vars` route with `badge-fallback` (no vars) and `badge-bound` (vars injected via unquoted-selector `<style>`) sections.
- `harness/hono/src/podo/` + `.podo/`: generated by the installed tarball's own CLI (`dist/cli/index.js init` + `build --force`, log `:1662-1665`).
- `run8.sh` (shell-command logger) and `abh8.sh` (agent-browser session-hono8 wrapper, `%q`-quoted full-command echo, no `sh -c` reinterpretation) added.
- `start.sh` route banner updated to list `/form-required`, `/xss-css`, `/badge-vars`, `/components.css`; re-verified accurate (its startup banner at log `:1671-1675`).
- Teardown performed: end-of-session re-hash of tarball + installed files unchanged (log `:2091-2094`), `✓ Browser closed` (log `:2095-2096`), `server pid killed`, port re-probe `000 <- port probe after kill (000 = refused)` (log `:2098-2100`).

---

# History appendix — previous rounds (SUPERSEDED)

Everything below predates session hono8, ran against OLDER artifacts, and is NOT evidence about tarball `634f24adaa889b613dbc1fee9e8f77c72ddf3d3e`. Retained only to document the trajectory; where a historical claim conflicts with the tables above, the hono8 result wins.

### R5 spot-check (session `hono6`; tarball sha1 `7064859bf44bc9bacf7b20fecb102d91287f3b0b`)
- Compact 4-check pass on the then-final artifact: `<Field required>` native `required` on bare Checkbox/Radio with blocked empty submit; Icon `size`/`decorative` spec attributes; `renderCriticalCss` css-param escape (`/xss-css`); quick `/` + `/form` regression. 4/4 PASS (its banner at log `:1416`; screenshots `j01…j05`; raw bodies `hono6-raw-*`).
- Added the `/form-required`, `/xss-css` routes and the `icon-spec` section that hono8 re-used. Its icon evidence quoted `data-size` attributes but NOT computed pixel sizes — the harness still forced `font-size: 24px` on `.icon-row i` then; hono8 item 15 removed the override and proved computed 16/24/32.
- Its gate run was `Tests 276 passed | 2 skipped (278)`; its first gate attempt failed due to a concurrent `pnpm check` race in `podo-ui.test.ts` `beforeAll` (two interleaved `node build.mjs` runs), solo re-run green.
- Freshness marker then: `dist/hono/index.js` sha1 `94d873cc…` (unchanged into the frozen artifact), `styles.css` `55d4f4e3…` (changed since — hono8 measured `fa65c26a…`).

### Round 5 (session `hono5`; tarballs `02fbd90e…` → statement extended to `76b395ba…`)
- The previous "definitive" full pass: 22/22 (14 prior-checklist re-runs + 8 new items: Field generic slot around Checkbox/Switch/Select/Radio/Button, Field invalid propagation without child props, disabled-OR `disabled={false}` escape attempt, Switch `aria-disabled`, Toast closable+suffixIcon routing, all-selects-named audit, disabled Textarea, real-keystroke attacks on disabled/readOnly controls).
- All of its still-relevant items were RE-RUN in hono8 (items 1–14, 18) — none are carried forward on hono5's evidence. The real-keystroke disabled/readOnly typing checks were hono5-only and were not part of the hono8 checklist; the equivalent guarantees are covered by hono8's Tab-skip, submit-payload (disabled absent, readOnly present), and reset evidence.
- Its environmental artifact (shell command-substitution mangling eval JS under `sh -c`) motivated hono8's `abh8.sh` pass-through wrapper.
- A post-report note extended its evidence to tarball `76b395ba…` (native-only Select fix; no hono-consumed file changed), and the R5 spot-check refreshed it against `7064859b…`.

### Round 4 (session `hono4`; tarball sha1 `af9c435a…`)
- First "definitive" round: 14/14 of its checklist (screenshots `g01…g08`, raw bodies `hono4-raw-*`). Confirmed the checkbox-repaint fix (`.podo-checkbox:checked` present) that hono3 had flagged.
- Its eval-scope `const`-redeclare tool artifact informed the IIFE-only eval style used ever since.

### Round 3 (session `hono3`; tarball sha1 `b7002808…`)
- Reported 10.5/11; its headline defect — checkbox visuals not tracking user toggles (CSS keyed only off `[data-state="checked"]`) — was fixed in the hono4-era artifact and re-proven on every artifact since (hono8 item 6).
- Introduced full-eval-JS logging and the `f0*.png` screenshots; established that `fill` bypasses readonly (hence real `keyboard type` for all clip/readonly assertions since).

### Rounds 1–2 (session `hono`; original 2.0.4 build) and interim `honofix2`
- Round 1 established the harness; its "17/17 exports pass" claim was later qualified (icon glyphs rendered blank — fixed by the `podo-ui/icons.css` export, re-proven in hono8 item 1; checkbox repaint defect missed).
- Round 2's "dist byte-identical, no browser re-run needed" reasoning became obsolete when `@podoui/hono` changed; superseded by full fresh runs in every later round.
- `honofix2` verified a checkbox-repaint candidate build (`16b6e55f…`) that was itself replaced.
- Historical screenshots retained in `reports/hono-shots/`: `01…03` (round 1), `f01…f09` (round 3), `g01…g08` (round 4), `h01…h08` (round 5), `j01…j05` (hono6).

## Final artifact delta (tarball b691f35c3c93…)

Two findings from the frozen round (session *8) were fixed after it:
1. The default `podo build` now emits the Badge binding vars: the CLI's
   defaultComponentDocuments embeds the spec badge document and the default
   token document gained the `color.palette.*` tokens it references (values ==
   the styles.css fallback hexes). Regression-locked by the CLI build test
   (`--podo-badge-root-background:` / `.podo-badge[data-theme="red"]` /
   `--podo-color-palette-error-5: #FEF1F1` assertions).
2. The Editor's unwired second hidden file input was removed (uploads into it
   were silently inert). Regression-locked by the editor test
   ("renders exactly one file input, wired to the image upload flow").
Repo gate on the final artifact: 23 files / 327 passed | 2 skipped (329).
Micro-verification in the react harness (session microv, logged commands):
fresh install + `podo build --force` → components.css contains 33
`--podo-badge-*` declarations (e.g. `[data-theme="red"] {
--podo-badge-dot-color: var(--podo-color-palette-accent-50); … }`), tokens.css
defines the palette vars; in-browser, the danger Badge's
`--podo-badge-root-background` computes to `#F23B3B` with backgroundColor
rgb(242,59,59) (previously the var computed empty), and each of the 3 mounted
Editors renders exactly one `input[type=file]` (was two each). No other module
changed: only dist/cli and dist/react/editor differ from 634f24ad.

## Closing delta (final tarball 9da8f7ec6667…)

Nine last real bugs from adversarial round 6 were fixed with red→green tests
(repo gate: 23 files / 340 passed | 2 skipped = 342): uncontrolled DatePicker
committed values now render (+`defaultValue` prop; browser-verified in the
react harness: placeholder → "2026 - 07 - 15" after an uncontrolled pick);
Select chip-remove/clear-all keyboard activation no longer swallowed by the
trigger handler; controlled/forced-open Tooltips re-measure on scroll/resize;
re-clicking a selected Editor image keeps a single wrapper; SSR aria-expanded/
aria-controls now track the portal menu's actual presence; native Select
clear-all routes through the stale-event lock; labeled dot Badges are real
accessibility elements (native accessible+image role, hono role="img");
hono Tooltip accepts an id for the spec's aria-describedby contract.
Remaining adversarial-round-6 items are NOT bugs and are catalogued as:
feature backlog (native uncontrolled-state APIs, Field label-press focus,
DatePicker enable/disable-function browser fixtures, Editor toolbar-filter/
resize fixtures, Tooltip position fixtures), design questions (Typography
variant default derived from `as` — consistent react/hono behavior), and
environment-bound evidence (real iOS/Android assistive-technology behavior —
RNW + unit tests are the attainable proxy here).
