# Podo UI v2 — native runtime FROZEN-ARTIFACT FINAL verification, session `native8` (react-native-web harness)

- Date: 2026-07-21 — session **`native8`** (fresh `agent-browser --session native8` for every step via `harness/native/ab8.sh`, which logs the FULL command line including complete eval JS and full output to `reports/native-commands.log` under the banner `=== FROZEN FINAL VERIFICATION session native8 tarball 634f24adaa889b613dbc1fee9e8f77c72ddf3d3e ===`). This document supersedes every prior round (`native`–`native7`) — all preserved in the History appendix.
- **Artifact under test (FROZEN)**: `podo-ui-2.0.4.tgz` **sha1 `634f24adaa889b613dbc1fee9e8f77c72ddf3d3e`** — hashed as the FIRST logged action of the session. Every claim in this document was produced in session native8 against this exact tarball.
- **Clean reinstall** before any browser step: `rm -rf node_modules/podo-ui node_modules/.vite package-lock.json && npm install ../../podo-ui-2.0.4.tgz` → installed `2.0.4`, `found 0 vulnerabilities`.
- **Bit-for-bit freshness proof** (all hashes in `native-commands.log`):
  - installed `node_modules/podo-ui/dist/native/index.js` sha1 **`7b4108f5f20b354f0299eb2b33d2ddf2531289d9`** == tarball extraction (`tarball-inspect-native8/package/dist/native/index.js`) == repo build (`/Users/tarucy/project/podoui/packages/podo-ui/dist/native/index.js`). **This file CHANGED this round** — the native7 tarball's copy hashed `ee81607838ae8adc3c6a8c1e0e173c4997ff6dfa`.
  - installed `node_modules/podo-ui/dist/react/index.js` sha1 **`06c2e00766f61be352a404469518d691fe749dae`** == tarball extraction.
  - Round-unique markers in the installed `dist/native/index.js`: `aria-haspopup` ×2, `aria-pressed` ×2, `tooltip` ×4 — each greps **0** in the native7 tarball copy. Carried markers: `assertive` ×2, `"status"` ×3, `decorative` ×3, `suppressSynthPress` ×14, `iconGlyphs` ×4.
- **Repo consulted READ-ONLY**. Repo gate: `check-final.log` (2026-07-21 12:11) — `pnpm check` green: `Test Files  23 passed (23)` / `Tests  326 passed | 2 skipped (328)` (lines 73–74). Additionally re-proven IN THIS SESSION: `npx vitest run packages/native --reporter=verbose` → **34/34 passed** (full verbose list in `native-commands.log`, session-native8 banner).
- Harness: Vite + React 19 + react-native-web 0.21 at `http://127.0.0.1:5214` (`harness/native/start.sh`; server killed at end — "server down confirmed (native8 final)" — and `agent-browser close` run). Host injection: `createNativeComponents({ Pressable, ScrollView, Text, TextInput, View })` with real react-native-web components. Harness extended this session (harness only, repo untouched): new `#icon-contract-section` (Icon `size` sm/md/lg/default + `decorative={false}` labeled icon) and a `switch-events` counter on `switch-main` (proves Enter toggles exactly once).
- Screenshots: `reports/native-shots/m01`–`m19` (m-prefix = session native8).

## Result: **24/24 checklist items PASS** (16 established re-run + 8 new round-6 contract checks)

Zero tab resets and zero click storms this session; `agent-browser errors` was empty at open, at the console sweep, and immediately before close. Two first-read probe-selector errors occurred and were re-read cleanly (Environmental artifacts §1–2) — every PASS cites only the clean reads.

## Part A — the established 16, re-run end-to-end in session native8

| # | What was exercised | Result | Evidence (quoted from `native-commands.log`, session native8) |
|---|---|---|---|
| A1 | Provider theme/colorScheme/tokens flip + `adaptReactNativeTokens` 7 probes | PASS | Light: `{"themeReadout":"dashboard:light","inputColor":"rgb(17, 24, 39)","inputWrapperBg":"rgb(255, 255, 255)","inputWrapperBorder":"rgb(228, 228, 231)","fieldGap":"12px"}` (gap 12 = provider token `"0.75rem"`). After toggle: `{"themeReadout":"dashboard:dark","inputColor":"rgb(248, 250, 252)","inputWrapperBg":"rgb(16, 24, 40)","inputWrapperBorder":"rgb(62, 66, 75)","btnPrimaryBgDark":"rgb(66, 108, 237)"}`; toggle back `"dashboard:light"`. Tokens: `tok-px 16:number`, `tok-rem 24:number`, `tok-neg -4:number`, `tok-hex "#FFFFFF":string`, `tok-pct "50%":string`, `tok-nested {"size":16,"gap":24,"color":"#FFFFFF","nested":[8,{"inner":32}]}:object`, `tok-array [2,8,5,"auto"]:array`. Shots m01/m02 |
| A2 | Button 8 themes + 4 sizes; onPress; disabled flat treatment; keyboard; pressed fill | PASS | All 8 themes distinct (solid-primary `rgb(66, 108, 237)`, solid-assistive `rgb(244, 244, 245)`, solid-white `rgb(255, 255, 255)`, solid-danger `rgb(242, 59, 59)`, outline-primary border `rgb(66, 108, 237)`, outline-assistive border `rgb(209, 210, 214)`, outline-white `rgb(255, 255, 255)`/border `rgb(209, 210, 214)`, outline-danger border+label `rgb(242, 59, 59)`). Sizes xs 32/6px, sm 36/8px, md 42/10px, lg 52/12px (measuredH == minHeight all four). 2 clicks → `"btnClicks":"2"`. Disabled: `{"btnDisabledClicks":"0","disabledDom":{"tag":"BUTTON","ariaDisabled":"true","disabledAttr":true,"tabindex":"-1","bg":"rgb(228, 228, 231)","labelColor":"rgb(159, 162, 173)","rootOpacity":"1"},"anyNonUnitOpacityInChain":[]}` — flat colors, zero opacity dimming anywhere in the chain. Keyboard: `{"activeIsBtnMain":true,"activeTag":"BUTTON"}` → Enter → Space → `"4"` (each key exactly one activation). Pressed (real held mousedown): resting `rgb(66, 108, 237)` → `{"bgDuringHold":"rgb(18, 59, 186)"}` = `#123BBA` (shot m03 taken DURING the hold) → `{"bgAfterRelease":"rgb(66, 108, 237)"}` |
| A3 | Chip toggle/onPress/removable/removable+disabled/disabled/themes | PASS | Toggle press 1 → `{"ariaPressed":"true","selectedReadout":"true","pressEvents":"1","bg":"rgb(62, 66, 75)"}`; press 2 → `{"chipSelected":"false","chipPressEvents":"2"}`. Disabled: `{"chipDisabledEvents":"0","disabledDom":{"tag":"BUTTON","ariaDisabled":"true","tabindex":"-1","bg":"rgb(228, 228, 231)"}}`. Removable ✕ quoted `<button aria-label="필터 제거" role="button" tabindex="0" …>` (white ✕ on the `#3E424B` selected-look fill — removable chips are born selected per `index.tsx:601-611`); click → `{"chipRemoved":"1","removableStillInDom":0}`. Removable+disabled: root `{"tag":"DIV","isButton":false,"bg":"rgb(228, 228, 231)"}`, label `rgb(159, 162, 173)`, ✕ quoted `<button aria-disabled="true" disabled="" aria-label="잠긴 제거" … tabindex="-1" …>`; click → `{"chipDisabledRemoveEvents":"0"}`. Themes: solid `rgb(244, 244, 245)`, outline-strong border `rgb(228, 228, 231)`, outline-weak selected border `rgb(118, 121, 133)`, lg label `16px` |
| A4 | Badge all 12 themes + dot | PASS | natural `rgb(62, 66, 75)/rgb(249, 249, 249)`, danger `rgb(242, 59, 59)`, success `rgb(62, 168, 86)`, warning `rgb(255, 170, 0)`, info `rgb(0, 149, 255)`, gray `rgb(244, 244, 245)/rgb(24, 24, 27)`, red `rgb(254, 241, 241)/rgb(242, 59, 59)`, green `rgb(236, 248, 239)/rgb(62, 168, 86)`, yellow `rgb(255, 247, 230)/rgb(255, 170, 0)`, blue `rgb(235, 245, 255)/rgb(0, 149, 255)`, purple `rgb(248, 245, 255)/rgb(142, 81, 255)`, orange `rgb(255, 244, 240)/rgb(255, 106, 51)`; dot `{"bg":"rgb(241, 87, 100)","w":"6px","h":"6px","ariaLabel":"새 알림"}`. Shot m05 |
| A5 | Icon glyph/name fallback/color + provider `iconGlyphs` map | PASS | `{"iconMenu":"≡","iconNameOnlyResolved":["U+E009"],"iconColor":"rgb(17, 24, 39)"}`. Map: `mapped_search_nameOnly {"text":"","codes":["U+E009"]}`, `mapped_menu ["U+E001"]`, `unmapped_close_fallsBackToName {"text":"close"}`, `explicitGlyphBeatsMap {"text":"★","codes":["U+2605"]}` — resolution `glyph ?? theme.iconGlyphs?.[name] ?? name` |
| A6 | Select single: combobox trigger; mouse open/pick; reopen shows selection | PASS | Trigger (open) quote: `<div aria-expanded="true" aria-haspopup="listbox" aria-label="과일" role="combobox" tabindex="0" … aria-activedescendant="podo-select-_r_1_-option-0" aria-controls="podo-select-_r_1_" …>`. 3 cells `DIV role="option"` with ids `podo-select-_r_1_-option-0/1/2`; click 딸기 → `{"selectValue":"strawberry","triggerText":"딸기▾","menuStillOpen":0,"ariaExpanded":"false","ariaControls":null}`. Reopen: `{"activeDescendant":"podo-select-_r_1_-option-0","ariaSelectedFlags":["true","false","false"],"selectedCellBg":"rgb(244, 244, 245)","optionTabindexes":["-1","-1","-1"]}`. Structural note vs prior rounds: the `testID` now sits on the ROOT wrapper (trigger + menu); the combobox child carries all ARIA — selectors updated accordingly (both reads in the log). Shot m06 |
| A7 | Select multi: 3 picks / chip-remove / clear-all; disabled locked; readOnly | PASS | Picks (one per eval) → `{"multiValues":"jeju,seoul,busan","triggerText":"제주✕서울✕+1✕▾","removeLabels":["제주 제거","서울 제거","모두 해제"],"nestedButtonPairs":0,"menuAriaSelected":["true","true","false","true"]}` (maxChips=2 → `+1`); menu `aria-multiselectable="true"`. ✕ 제주 제거 → `"seoul,busan"`; 모두 해제 → `{"multiValues":"(none)","triggerText":"도시 선택▾"}`. Disabled: `{"trigTag":"DIV","role":"combobox","ariaLabel":"잠긴 도시","ariaDisabled":"true","tabindex":"-1","ariaHaspopup":"listbox","ariaExpanded":"false","trigText":"서울부산▾","buttonsInsideTrigger":0,"trigBg":"rgb(228, 228, 231)","trigBorder":"rgb(209, 210, 214)"}`; click → `{"disabledEvents":"0","menuCells":0,"valuesStill":"서울부산▾"}`. readOnly: `{"trigText":"대구제주","hasChevron":false,"buttonsInsideTrigger":0,"bg":"rgba(0, 0, 0, 0)","borderColor":"rgba(0, 0, 0, 0)"}`; click → `{"readonlyEvents":"0","menuCells":0}`. Shots m07/m08 |
| A8 | Select transition: `disabled` flips WHILE open → instant close; stale inert; re-enable stays closed; still functional | PASS (every flip guarded by an in-page click counter) | 8-1 open: `{"lockDisabled":"false","cells":3,"cellTexts":["딸기","바나나","포도"],"trigBorder":"rgb(66, 108, 237) / 2px","ariaExpanded":"true","staleCellSaved":true,"strayToggleClicks":0}`. 8-2 flip: `{"toggleClicksDispatched":1,"lockDisabled":"true","cellsAfterFlip":0,"menuGone":true,"trigAriaDisabled":"true","trigTabindex":"-1","trigAriaExpanded":"false","trigBg":"rgb(228, 228, 231)","trigBorder":"rgb(209, 210, 214) / 1px","lockEvents":"0"}`. 8-3 stale: `{"staleCellStillConnected":false,"lockEvents":"0","lockValue":"(none)","cells":0}` (saved cell `.click()` + MouseEvent + disabled-trigger click all inert). 8-4 re-enable: `{"toggleClicksTotal":2,"lockDisabled":"false","staysClosed":true,"trigAriaDisabled":null,"lockEvents":"0"}`. 8-5 reopen 3 cells; pick 바나나 → `{"lockValue":"banana","lockEvents":"1","menuOpen":0}` (fresh-eval read). Unit corroboration: `closes the open Select menu when disabled or readOnly flips, and blocks stale picks` (index.test.tsx:858) passed in-session. Shots m09/m10 |
| A9 | Input: typing; invalid; disabled treatment + type attempt; readOnly boxless; required | PASS | Typing: `{"inputReadout":"12500","domValue":"12500","wrapperText":"₩원✕","invalidBorder":"rgb(242, 59, 59)","normalBorder":"rgb(228, 228, 231)"}`. Disabled before: `{"value":"locked","readOnlyAttr":true,"disabledAttr":false,"tabindex":"-1","wrapperBg":"rgb(228, 228, 231)","wrapperBorder":"rgb(209, 210, 214)","controlColor":"rgb(159, 162, 173)"}`; `type` "HACK" → `{"disabledValueAfterTypeAttempt":"locked"}`. Tab order: focus `input-invalid` → Tab → `{"afterTabFromInvalid":"input-readonly","tabSkippedDisabled":true}`; honesty: programmatic `el.focus()` still lands (`focusAttemptLandedOnDisabled:true` — DOM-standard for `tabindex="-1"`). readOnly: `type` "HACK" → `"read only"` unchanged, boxless `{"wrapperBorder":"rgba(0, 0, 0, 0)","wrapperBg":"rgba(0, 0, 0, 0)","paddingLeft":"0px"}`. Required: full attr list quoted (`aria-label="필수 값"`, `ariaRequired:null` — RNW drops TextInput accessibilityState); fiber walk `{"foundAtHop":3,"accessibilityState":{"disabled":false,"invalid":false,"required":true},"editable":true}`; unit `wires required into standalone Input and Textarea accessibilityState` (index.test.tsx:435) passed in-session |
| A10 | Textarea: typing/count; invalid; disabled + readOnly type attempts | PASS (clean rerun on the control itself — artifact §1) | `{"textareaValue":"여러 줄 소개 텍스트","textareaLength":"11"}`; control-level styles: main `{"tag":"TEXTAREA","rows":"4","minHeight":"78px","border":"rgb(228, 228, 231)"}`, invalid `{"border":"rgb(242, 59, 59)"}`, disabled `{"value":"locked memo"` after HACK type attempt`,"readOnlyAttr":true,"tabindex":"-1","bg":"rgb(228, 228, 231)","border":"rgb(209, 210, 214)","color":"rgb(159, 162, 173)"}`, readOnly `{"value":"read only memo"` unchanged`,"border":"rgba(0, 0, 0, 0)","bg":"rgba(0, 0, 0, 0)","paddingLeft":"0px"}`. Tab: focus `textarea-invalid` → Tab → `{"afterTabFromTextareaInvalid":"textarea-readonly","tabSkippedDisabledTextarea":true}`. Shot m14 |
| A11 | Field wiring; disabled/invalid propagation; OR semantics; distinct auto ids; explicit id wins; countMax + external sync; describedBy join | PASS | Wiring: `{"emailAttrs":{"ariaLabelledBy":"fld-email-label","ariaDescribedBy":"fld-email-description"},"labelEl":{"text":"이메일"},"descEl":{"text":"업무용 메일을 입력해 주세요"},"nick":{"ariaDescribedBy":"fld-nick-error","errorText":"필수 항목이에요","errorColor":"rgb(242, 59, 59)"}}`, nick wrapper border `rgb(242, 59, 59)` (clean re-read, artifact §2). Counter: `{"counterBefore":"0/20"}` → 1 click `btn-memo-preset` (external controlled update, no typing) → `{"counterAfter":"10/20","memoReadout":"외부에서 갱신된 값","valueLength":10}`. Field disabled (prop-less child): `{"value":"","readOnlyAttr":true,"tabindex":"-1","wrapperBg":"rgb(228, 228, 231)"}`; HACK → `{"domValue":"","valueReadout":"(empty)"}`. Field invalid (prop-less child): `{"wrapperBorder":"rgb(242, 59, 59)","fiberAccessibilityState":{"invalid":true,"required":false,"disabled":false},"childAriaDescribedBy":"podo-field-_r_e_-error","errorText":"Field invalid 전파"}`. OR semantics (child `disabled={false}` in disabled Field): `{"value":"","readOnlyAttr":true,"tabindex":"-1","wrapperBg":"rgb(228, 228, 231)","wrapperBorder":"rgb(209, 210, 214)","controlColor":"rgb(159, 162, 173)"}`; HACK → `{"domValue":"","valueReadout":"(empty)"}` — the opt-out loses. Auto ids: `autoA podo-field-_r_b_-label` / `autoB podo-field-_r_c_-label`, `distinct:true`, label texts 이름/주소, `explicitWins:true`. Join: `{"ariaDescribedBy":"ext-hint podo-field-_r_g_-description"}`, both ids resolve, `childOwnIdFirst:true`, `fieldIdJoinedAfter:true`. Shot m15 |
| A12 | Switch/Checkbox/Radio: mouse toggles + disabled no-ops | PASS | Switch: click → `{"switchValue":"true","switchEvents":"1","dom":{"role":"switch","tag":"DIV","trackBg":"rgb(66, 108, 237)","handleMargin":"14px"}}` → click → `{"switchValue":"false","switchEvents":"2"}`; disabled → `{"switchDisabledEvents":"0","ariaDisabled":"true","disabledTrack":"rgb(228, 228, 231)"}`. Checkbox: B click → `{"values":"A:true,B:true","parentMark":"✓전체 선택"}`; parent click → `"A:false,B:false"`; disabled → `"0"`. Radio: pro → `"pro"`; max → `{"radioValue":"max","dots":[2,2,3]}` (only max renders the inner dot node); disabled → `{"events":"0","valueUnchanged":"max","ariaDisabled":"true","tabindex":"-1","circleBg":"rgb(228, 228, 231)"}`. Shot m16 |
| A13 | Toast show/dismiss; Tooltip layout | PASS (in-page counter proves exactly one click per step) | Show: `{"showClicksDispatched":1}` → `{"toastVisible":"true","live":{"role":"status","ariaLive":"polite","bg":"rgb(236, 248, 239)","border":"rgb(62, 168, 86)","text":"✔저장됐어요실행 취소✕캡션 영역"}}` (live success card now announces politely — the round-6 B2 contract; prior rounds' `role="alert"` is superseded). Dismiss via quoted `<button aria-label="닫기" …>` → `{"toastVisible":"false","toastClosed":"1","strayShowClicks":1}`. 5 static states quoted in B2 below. Tooltip: top `{"flexDir":"column-reverse","align":"center","text":"임시 저장돼요"}`, reverse `{"bubbleBg":"rgb(255, 255, 255)","labelColor":"rgb(24, 24, 27)"}`, bottom `{"flexDir":"column","align":"flex-end"}`. Shots m17/m18 |
| A14 | Top-level convenience exports (defaultNativeHost string tags) | PASS | DOM quote `<pressable accessibilityrole="button" accessibilitystate="[object Object]" testid="conv-btn" data-theme="solid-primary" data-size="md" style="…background-color: rgb(66, 108, 237)…">`; `.click()` → `{"convClicksAfterClick":"0"}` (`onPress` inert under react-dom — documented contract), `convError:0`. Unit citation (browser cannot enumerate all 13): `renders all 13 top-level exports against the defaultNativeHost string tags` (index.test.tsx:1187) — passed in this session's 34/34 run |
| A15 | Console/page errors | PASS | `errors` empty at open, mid-session, and at close (blank outputs quoted in the log). Structure sweep: `{"nestedButtonPairs":0,"totalButtons":27,"comboboxes":7,"listboxesOpen":0,"decorativeIconCount":12,"allDecorativeAriaHidden":true,"labeledIconNotHidden":true}`. Full categorized dump in B8. Shot m19 |
| A16 | Honesty section | PASS | Written below — RNW attribute drops, what RNW cannot prove vs real RN, probe-selector artifacts, and the superseded toast-role note |

## Part B — NEW round-6 contract checks

| # | Check | Result | Evidence (quoted from `native-commands.log`, session native8) |
|---|---|---|---|
| B1 | Icon size → fontSize 16/24/32; `decorative={false}`+label → role img; default hidden trio | PASS | Sizes: `{"icon-size-sm":"16px","icon-size-md":"24px","icon-size-lg":"32px","icon-size-default":"24px"}`. Labeled quote: `<div dir="auto" aria-label="검색 아이콘" role="img" class="css-text-146c3p1" data-testid="icon-labeled" …>⌕</div>` — `role="img"`, label present, NO `aria-hidden`. Default hidden quote: `<div dir="auto" aria-hidden="true" class="css-text-146c3p1" data-testid="icon-menu" …>≡</div>`; page-wide `allDecorativeAriaHidden:true` (12 decorative icons). The iOS (`accessibilityElementsHidden`) / Android (`importantForAccessibility:"no-hide-descendants"`) legs of the trio are not renderable in RNW — unit citations: `hides Icon from the iOS, Android, and web accessibility trees` (index.test.tsx:616), `maps the Icon size variant to the 16/24/32 glyph fontSize` (:934), `exposes a meaningful Icon as a named image instead of hiding it` (:951) — all passed in-session |
| B2 | Toast roles: success/info/warning/normal → `role="status"` + polite; danger → alert + assertive | PASS | All five probed: normal/success/info/warning each `{"role":"status","ariaLive":"polite"}`, danger `{"role":"alert","ariaLive":"assertive"}`. DOM quotes: success `<div aria-live="polite" role="status" … data-testid="toast-success" style="…background-color: rgb(236, 248, 239); border-color: rgb(62, 168, 86)…">` vs danger `<div aria-live="assertive" role="alert" … data-testid="toast-danger" style="…background-color: rgb(254, 241, 241); border-color: rgb(242, 59, 59)…">`. The LIVE success toast (A13) also announced `role="status"`/polite. Unit corroboration: `announces Toast as status per state and as alert only for danger` (index.test.tsx:984) |
| B3 | Chip `aria-pressed` reflects toggle state; pressed visual during a real held mousedown | PASS | Both states quoted (full outerHTML in the log): unselected `<button aria-pressed="false" role="button" tabindex="0" … data-testid="chip-toggle" … background-color: rgb(244, 244, 245)…>` → click → `<button aria-pressed="true" … background-color: rgb(62, 66, 75)…>`. Held mousedown on the SELECTED chip: `{"bgDuringHold":"rgb(118, 121, 133)","ariaPressedDuringHold":"true"}` = the styles.css `:active` selected-solid fill `#767985` (shot m04 taken during the hold); release → `{"bg":"rgb(62, 66, 75)","pressEvents":"1"}` (the synthetic hold registered zero presses — state untouched). Unit corroboration: `exposes the Chip toggle via aria-pressed with the styles.css pressed feedback` (index.test.tsx:1014) |
| B4 | Switch Enter toggles EXACTLY once (synthesis guard) + Space toggles; Checkbox Space; Radio Space selects | PASS (focus + url asserted around every key) | Switch (`role="switch"` DIV): before `{"value":"false","events":"2"}` → Enter → `{"value":"true","events":"3"}` — **counter +1, not +2**: the handled Enter swallowed RNW's synthesized press (a double would net value unchanged and events +2) → Space → `{"value":"false","events":"4"}`. Checkbox (`role="checkbox"` DIV): focus A, `before "A:false,B:false"` → Space → `{"values":"A:true,B:false"}`; Enter (unhandled → lone synthesized press) → `"A:false,B:false"` — single toggle. Radio (`role="radio"` DIV): focus basic with `before "max"` → Space → `{"radioValue":"basic","spaceSelected":true}`. Unit corroborations: `toggles the Switch from the keyboard and swallows Enter's synthesized press` (index.test.tsx:1060), `toggles the Checkbox and selects the Radio with Space` (:1098) |
| B5 | Select trigger wiring: `aria-haspopup="listbox"`, `aria-controls`=menuId while open, `aria-expanded` true/false; Tab from the trigger SKIPS the options | PASS | Open: `{"ariaHaspopup":"listbox","ariaExpanded":"true","ariaControls":"podo-select-_r_1_","menuId":"podo-select-_r_1_","controlsMatchesMenuId":true,"activeDescendant":"podo-select-_r_1_-option-0"}`; after pick/close: `{"ariaExpanded":"false","ariaControls":null,"ariaHaspopup":"listbox"}` (haspopup persists; controls only while open). Tab-skip activeElement sequence: focus single trigger with menu OPEN (`{"focusedIsTrigger":true,"menuOpen":1}`) → Tab → `{"activeTag":"DIV","activeRole":"combobox","nearestTestid":"select-multi","landedOnOption":false,"stillInsideSingleSelect":false}` — focus lands PAST the select on the next combobox; every option `tabindex="-1"` (`optionTabindexes:["-1","-1","-1"]`). Unit corroboration: `wires the Select trigger to its listbox and keeps options out of the focus order` (index.test.tsx:1141) |
| B6 | Tooltip bubble `role="tooltip"` with a referenceable id | PASS | Bubble quote: `<div role="tooltip" class="css-view-g5y9jx" id="podo-tooltip-_r_h_" style="background-color: rgb(62, 66, 75); border-radius: 8px; padding: 6px 8px;"><div dir="auto" …>임시 저장돼요</div></div>`; all three bubbles `role="tooltip"` with distinct ids (`podo-tooltip-_r_h_/_r_i_/_r_j_`), `document.getElementById(id)` resolves back to the bubble (`referenceable:true`). Unit corroboration: `labels the Tooltip bubble with the tooltip role and a referenceable id` (index.test.tsx:1176) |
| B7 | Regression: Enter/Space/Arrow Select flow still single-transition | PASS | Focus trigger `{"focusedIsTrigger":true,"ariaExpanded":"false","menuOpen":0}` → **Enter (closed)** → `{"cells":3,"ariaActivedescendant":"podo-select-_r_6_-option-0","activeOptionText":"딸기","menuOpen":1,"ariaExpanded":"true","focusStillTrigger":true}` — opens and STAYS open (one transition). ArrowDown ×2 → `-option-1`/바나나 → `-option-2`/포도, menu open throughout. **Enter (open)** → `{"kbdValue":"grape","kbdEvents":"1","triggerText":"포도▾","menuOpenAfterEnter":0,"cellsAfterEnter":0,"ariaExpanded":"false"}` — picks EXACTLY once and stays closed. Reopen via ArrowDown → active on the selection `포도✓`, `ariaSelectedFlags ["false","false","true"]`; ArrowUp → `-option-1`/바나나; Escape → `{"menuOpenAfterEscape":0,"kbdValueStill":"grape","kbdEventsStill":"1"}`. Mouse NOT swallowed: click open `{"menuOpen":1,"cells":3}` → click close `{"menuOpen":0}` → click open → pick 딸기 → `{"kbdValue":"strawberry","kbdEvents":"2","menuOpenAfterPick":0}`. Long-list scroll-follow re-proven: 15 options, menu `{"maxHeight":"474px","clientHeight":472,"scrollHeight":702,"overflowY":"auto"}` with the box styles on the scroll container (`padding:"0px"`) and `{"padding":"8px","rowGap":"4px","isDirectChild":true}` on the content container; 12× ArrowDown → `{"ariaActivedescendant":"podo-select-_r_7_-option-12","activeOptionText":"항목 13","scrollTopNow":230,"activeCellInsideMenuViewport":true}`; click last → `{"longValue":"item-15","menuClosed":true}`. Shots m11/m12/m13 |
| B8 | Final console sweep — documented categories only | PASS | 15 console lines for the session's single page load (no reloads needed this round; raw dump `reports/native8-console-raw.txt`): vite `[debug] connecting/connected` (×2), React DevTools `[info]` (×1), and ONLY the four documented convenience-export `[error]` categories from the `defaultNativeHost` string-tag demo — incorrect casing (Text ×2/View ×1/Pressable ×1), unrecognized tag (×3), `React does not recognize testID|accessibilityState|accessibilityRole|accessibilityLabel` (×4), `Unknown event handler property onPress` (×1). `grep -ciE "descendant|nested|cannot be a"` → **0**. `agent-browser errors` empty at open, at the sweep, and at session end |

## Console section

Raw dump: `reports/native8-console-raw.txt` (15 lines, one page load). Categorized `uniq -c` (in the log): 4× does-not-recognize-prop, 3× unrecognized-tag, 4× incorrect-casing, 1× unknown-onPress — all from the intentional `defaultNativeHost` string-tag demo section (`#conv-section`), unchanged in kind and count-per-load since round E — plus 2× vite `[debug]` and 1× React DevTools `[info]`. No warnings or errors from any injected-host component. Page-error channel (`agent-browser errors`) empty at all three checkpoints.

## Environmental artifacts (probe/tool, not app — every affected readout re-read cleanly)

1. **Textarea wrapper mis-probe**: the first A10 style probe read `parentElement` of the TEXTAREA, which in this harness is the page `<section>` (harness CSS `border: 1px solid #ddd` → the bogus `rgb(221, 221, 221)` readings). Unlike Input (control inside a styled wrapper DIV), Textarea carries its box styles on the control itself. The ancestor-chain probe and the clean rerun on the control are both in the log; the PASS cites only the rerun.
2. **Nick invalid-border mis-probe**: first read took `borderColor` of the naked `<input>` (`rgb(0, 0, 0)`, no border); the danger border lives on its wrapper — clean re-read `rgb(242, 59, 59)` quoted.
3. No about:blank tab resets, no click storms, no same-eval read races this session (counters: `strayToggleClicks:0` before the A8 flip, `toggleClicksDispatched:1` after; `showClicksDispatched:1` / `strayShowClicks:1` around the toast flow). Prior rounds' artifact classes are documented in the History appendix.

## Honesty section — what this RNW harness can and cannot prove vs real React Native

- **Proven end-to-end here**: the component factory against the real RN prop contract in a driveable browser — controlled state round-trips, press/typing/removal/clear flows, the disabled-while-open Select transition with stale-press inertness, Field OR-semantics and describedBy joins, the full listbox contract (`role=listbox`/`option`, `aria-selected`, `aria-activedescendant`, and new this round `aria-haspopup`/`aria-controls`/`aria-expanded`), keyboard single-transition flows with the `suppressSynthPress` Enter guard (Select AND Switch), the 474px ScrollView cap with real internal scrolling + keyboard scroll-follow, pressed-state fills captured mid-hold (Button `#123BBA`, Chip `#767985`), Toast politeness split, Tooltip role/id, and disabled focus-order exclusion.
- **RNW keyboard contract**: real `<button>` elements (Button, chip-✕) activate on Enter AND Space natively. DIV-role Pressables now carry explicit `onKeyDown` handlers: Switch handles Enter (guarded) + Space; Checkbox and Radio handle Space and leave Enter to RNW's single synthesized press. On real RN there is no DOM keyboard — activation arrives via the native accessibility layer through the same injected `onPress` handlers, and the guard refs are inert by construction.
- **RNW attribute drops (quoted in the log)**: TextInput `accessibilityState` never renders (`aria-required`/`aria-invalid` absent — fiber walks + forwarding-host unit tests bridge the gap); `data-state` does not render on Pressable/View (structural probes used instead); the Switch's `accessibilityState.checked` did not surface as `aria-checked` on the DIV (state proven via the controlled readout + track color). The Icon's iOS/Android hidden props are not renderable — unit-cited (B1).
- **Superseded prior-round claim**: the live success Toast announced `role="alert"` through round native7; the round-6 contract deliberately narrows alert+assertive to `danger` only — the browser now shows `role="status"`/polite for the other four states (B2), matching `toast.component.json`.
- `aria-disabled`/`disabled`/`tabindex="-1"` on disabled Pressables and `tabindex="-1"` + DOM `readOnly` on disabled TextInputs ARE real DOM evidence. Programmatic `el.focus()` still lands on `tabindex="-1"` nodes (quoted) — DOM-standard; Tab-order exclusion is the meaningful proof. Native touch gesture timing and native focus order were not exercised — DOM click/key events only.

## Screenshots (`reports/native-shots/`, session native8 = `m`-prefix)

`m01-initial-light.png`, `m02-dark-mode.png`, `m03-solid-primary-pressed-hold.png` (taken DURING the mousedown hold — navy `#123BBA` fill visible), `m04-chip-selected-pressed-hold.png` (during hold — `#767985` fill), `m05-buttons-chips-badges.png`, `m06-select-single-open.png`, `m07-select-multi-3values.png`, `m08-select-disabled-readonly.png`, `m09-select-lock-open-before-flip.png`, `m10-select-lock-picked-after-reenable.png`, `m11-select-kbd-final.png`, `m12-select-long-open-capped.png`, `m13-select-long-scroll-follow.png` (항목 13 active inside the scrolled menu viewport), `m14-inputs-textareas.png`, `m15-fields.png`, `m16-switch-check-radio.png`, `m17-toast-open.png`, `m18-tooltips.png`, `m19-final-full.png`.

## How to reproduce

```sh
cd "/private/tmp/claude-501/-Users-tarucy-project-podoui/885735ab-d9d3-4bc8-9008-5a94bf821edb/scratchpad/harness/native"
shasum ../../podo-ui-2.0.4.tgz                       # 634f24adaa889b613dbc1fee9e8f77c72ddf3d3e (FROZEN artifact)
rm -rf node_modules/podo-ui node_modules/.vite package-lock.json && npm install ../../podo-ui-2.0.4.tgz
shasum node_modules/podo-ui/dist/native/index.js     # 7b4108f5f20b354f0299eb2b33d2ddf2531289d9 (changed this round; native7 was ee81607838…)
shasum node_modules/podo-ui/dist/react/index.js      # 06c2e00766f61be352a404469518d691fe749dae
grep -c aria-haspopup node_modules/podo-ui/dist/native/index.js   # 2 (round-6 marker; 0 in the native7 build)
sh start.sh                                          # Vite on http://127.0.0.1:5214
./ab8.sh open http://127.0.0.1:5214                  # ab8.sh = logging wrapper: npx agent-browser --session native8
# … drive checks (full command lines + outputs: reports/native-commands.log, session-native8 banner)
(cd /Users/tarucy/project/podoui && npx vitest run packages/native --reporter=verbose)   # 34/34; pnpm check green: 23 files / 326 passed | 2 skipped = 328 (check-final.log:73-74)
./ab8.sh close && kill $(cat server.pid)             # teardown (done: "server down confirmed (native8 final)")
```

## Command log

Every `agent-browser` invocation — full command line (with complete eval expressions) and full output — is in `reports/native-commands.log` under `=== FROZEN FINAL VERIFICATION session native8 tarball 634f24adaa889b613dbc1fee9e8f77c72ddf3d3e ===` (tarball shasum, clean reinstall, bit-for-bit freshness proofs, all 24 checks, the two probe-artifact annotations with their clean reruns, the console dump, the read-only 34/34 vitest run, and teardown).

---

## Fixture round (native9, tarball 1f56298f9cab674d…)

- Date: 2026-07-21 — session **`native9`** (`harness/native/ab9.sh` logs every full `npx agent-browser --session native9` command line + output to `reports/native-commands.log` under the banner `=== FIXTURE ROUND session native9 tarball 1f56298f9cab674d ===`). Scope: browser fixtures for the **2.1.0 uncontrolled-state APIs + Field label-press focus** (the items catalogued as "feature backlog" in the closing delta above). This round EXTENDS session native8 — it does not supersede it.
- **Artifact under test**: `podo-ui-2.1.0.tgz` sha1 **`1f56298f9cab674dcc8d7c787189674ad315767c`** — hashed as the first logged action. Clean reinstall (`rm -rf node_modules/podo-ui node_modules/.vite package-lock.json && npm install ../../podo-ui-2.1.0.tgz` → `2.1.0`, 0 vulnerabilities). Bit-for-bit freshness: installed `dist/native/index.js` sha1 **`1bb65c59abbef56df38c899baf2376ec763c8f03`** == tarball extraction (`tarball-inspect-native9/`) == repo build (`packages/podo-ui/dist/native/index.js`); `dist/react/index.js` `d83db537…` == tarball. Round-unique markers in the installed file (each **0** in the native8 tarball copy): `defaultSelected` ×1, `defaultChecked` ×3, `defaultValues` ×1, `controlRef` ×6. Carried: `suppressSynthPress` ×14, `aria-pressed` ×2, `iconGlyphs` ×4. The native harness consumes only the `podo-ui/native` ESM entry — no `podo build` artifacts — so `podo build --force` is n/a here (react/hono harnesses are the CSS consumers).
- **Repo READ ONLY**. Gate: `pnpm check` green — `Test Files  23 passed (23)` / `Tests  349 passed | 2 skipped (351)`, exit 0 (`check-fixture9.log:73-74`). In-session focused run: `npx vitest run packages/native --reporter=verbose` → **44/44** (native8 had 34), including the new `defaultSelected/defaultChecked/defaultValue(s)/label-press` unit tests (verbose list in the log).
- Harness extended (harness only, repo untouched): new `#uncontrolled-section` (chip-un, chip-ctl-locked, switch-un, checkbox-un, radio-un, radio-un-default, select-un-single, select-un-multi, select-ctl-locked + change-callback readouts that never feed a value prop back) and `#field-focus-section` (fld-focus, fld-focus-disabled). One mid-session harness edit (added `radio-un-default`) followed by a full page reload; checks 1–3 evidence was captured pre-reload and stands on its logged quotes.
- Screenshots: `reports/native-shots/n01`–`n07` (n-prefix = session native9). Server killed at end ("server down confirmed (native9 final)") and `agent-browser close` run; `errors` empty at open, mid-session, and before close.

### Result: **7/7 fixture checks PASS**

| # | Check | Result | Evidence (quoted from `native-commands.log`, session native9) |
|---|---|---|---|
| F1 | Uncontrolled Chip `defaultSelected`: starts selected; press toggles (`aria-pressed` flips); controlled `selected` wins on the sibling | PASS | chip-un initial: `{"tag":"BUTTON","ariaPressed":"true","bg":"rgb(62, 66, 75)","labelColor":"rgb(255, 255, 255)"}` (selected `#3E424B` fill from `defaultSelected`, no `selected` prop). Press 1 → `{"ariaPressed":"false","bg":"rgb(244, 244, 245)","unChipLast":"false","unChipEvents":"1"}`; press 2 → `{"ariaPressed":"true","bg":"rgb(62, 66, 75)","unChipLast":"true","unChipEvents":"2"}`. Sibling chip-ctl-locked (`selected={false}` AND `defaultSelected`): initial `{"ariaPressed":"false","bg":"rgb(244, 244, 245)"}`; after 2 presses `{"ctlAriaPressed":"false","ctlBg":"rgb(244, 244, 245)","ctlChipEvents":"2"}` — callbacks fire but the controlled prop pins the state. (`data-state` not probed: RNW drops it on Pressable — documented in the native8 honesty section.) Shots n01/n02 |
| F2 | Uncontrolled Switch `defaultChecked` via press AND keyboard (Enter swallow intact, Space) | PASS | Initial: `{"role":"switch","trackBg":"rgb(66, 108, 237)"}` — ON from `defaultChecked` (`aria-checked` null = the documented RNW TextInput/Pressable state drop; state read via track color + callbacks). Press → `{"trackBg":"rgb(209, 210, 214)","last":"false","events":"1"}`. Focus (`focusedIsSwitchUn:true`) → **Enter** → `{"trackBg":"rgb(66, 108, 237)","last":"true","events":"2"}` — **counter +1, not +2**: the handled Enter swallowed RNW's synthesized press (a double would net OFF with events+2) → **Space** → `{"trackBg":"rgb(209, 210, 214)","last":"false","events":"3"}` |
| F3 | Uncontrolled Checkbox `defaultChecked` via press and Space; indeterminate fixture unchanged | PASS | Initial: `{"role":"checkbox","boxBg":"rgb(66, 108, 237)","mark":"✓"}` — checked from `defaultChecked`. Press → `{"boxBg":"rgb(255, 255, 255)","boxBorder":"rgb(159, 162, 173)","mark":null,"last":"false","events":"1"}`; focus + Space → `{"boxBg":"rgb(66, 108, 237)","mark":"✓","last":"true","events":"2"}`. Indeterminate fixture (controlled checkbox-parent) unchanged: `{"values":"A:true,B:false","mark":"–"}` → parent click → `{"values":"A:true,B:true","mark":"✓"}` → parent click → `{"values":"A:false,B:false","mark":null}` — same semantics as native8 A12 |
| F4 | Uncontrolled Radio `defaultChecked`: Space/press selects, never untoggles | PASS | radio-un starts unselected `{"circleBg":"rgba(0, 0, 0, 0)","circleBorder":"rgb(159, 162, 173)","hasDot":false}`; focus + **Space** → `{"circleBg":"rgb(66, 108, 237)","hasDot":true,"last":"true","events":"1"}`; two more presses → `{"circleBg":"rgb(66, 108, 237)","hasDot":true,"last":"true","events":"3"}` — callbacks keep reporting `true`, the radio **never untoggles**. Seed fixture radio-un-default (`defaultChecked`, no checked prop): starts `{"circleBg":"rgb(66, 108, 237)","hasDot":true}` and stays selected after a press. Shot n03 |
| F5 | Uncontrolled Select `defaultValue` (single, keyboard flow) + `defaultValues` (multi: pick/chip-remove/clear-all); controlled fixtures authoritative | PASS | **Single** (no `value` prop): trigger starts `딸기▾` from `defaultValue:"strawberry"`; focus + Enter opens with the internal selection active (`{"cells":3,"activeDescendant":"…-option-0","ariaSelected":["true","false","false"],"cellTexts":["딸기✓","바나나","포도"]}`); ArrowDown ×2 → `-option-2` → Enter → `{"triggerText":"포도▾","ariaExpanded":"false","menuCells":0,"unSelLast":"grape","unSelEvents":"1"}` — the label moved from internal state alone; reopen shows persistence (`ariaSelected ["false","false","true"]`, `포도✓`). **Multi** (no `values` prop): seeded trigger `서울✕✕▾` with `removeLabels ["서울 제거","모두 해제"]`; clear-all on the seed → `[]` (`"(empty)"`, events 1 — see the probe-artifact note); 3 picks (one eval each) → `{"triggerText":"서울✕부산✕+1✕▾","ariaSelected":["true","true","false","true"],"unSelMultiLast":"seoul,busan,jeju","unSelMultiEvents":"4"}` (maxChips=2 → `+1`, menu `aria-multiselectable:"true"`); ✕ 서울 제거 → `{"triggerText":"부산✕제주✕✕▾","unSelMultiLast":"busan,jeju","unSelMultiEvents":"5"}`; 모두 해제 → `{"triggerText":"비제어 도시▾","ariaSelectedInOpenMenu":["false","false","false","false"],"unSelMultiLast":"(empty)","unSelMultiEvents":"6"}`. **Controlled authoritative**: select-ctl-locked (`value="grape"` pinned) — pick 딸기 → `{"triggerText":"포도▾","menuOpen":0,"ctlSelectEvents":"1"}` (callback fired, value prop won); state-controlled select-single round-trips (`{"selectValueReadout":"strawberry","triggerText":"딸기▾"}`). Shots n04/n05 |
| F6 | Field label press focuses the wired Input; no-op on a disabled Field | PASS | Wiring: label `{"labelText":"포커스 라벨","labelId":"podo-field-_r_n_-label","labelIdMatches":true}`. Click the label → `{"activeTag":"INPUT","activeTestId":"input-focus","activeIsWiredInput":true}` with the activeElement quote `<input placeholder="라벨 프레스 포커스" … aria-describedby="podo-field-_r_n_-description" aria-labelledby="podo-field-_r_n_-label" class="css-textinput-11aywtz …>`. Disabled Field: blur to BODY, click `#podo-field-_r_o_-label` → `{"activeTag":"BODY","activeTestId":null,"landedInDisabledInput":false}` — no-op; the child input shows the propagated disabled treatment `{"readOnly":true,"tabindex":"-1","wrapperBg":"rgb(228, 228, 231)"}`. Shot n06 |
| F7 | Regression: console shows only the documented convenience-export categories | PASS | Raw dump `reports/native9-console-raw.txt` (35 lines across this session's 2 page loads + 1 HMR update): `[error]` lines categorize to EXACTLY the four documented convenience-export classes — incorrect casing ×12, unrecognized tag ×6, does-not-recognize prop ×8, unknown `onPress` ×2 — plus vite `[debug]` ×5 (2× connecting/connected + 1 hot update) and DevTools `[info]` ×2. `grep -ciE "descendant|nested|cannot be a"` → **0**. `agent-browser errors` empty at open, mid-session, and before close. Shot n07 |

### Environmental artifacts (probe/tool, not app)

1. **Multi-select center-click**: the first click aimed at the select-un-multi combobox landed on the clear-all ✕ nested inside the trigger (chips fill the center when values exist) — it did not open the menu, but legitimately exercised clear-all on the `defaultValues` seed (`[seoul]` → `[]`, callback `"(empty)"`, events 1; annotated in the log). The seed itself was quoted beforehand; the ordered pick/remove/clear flow then ran on the emptied trigger.
2. One intentional harness edit mid-session (added `radio-un-default`) + full reload — noted in the log; no tab resets, no click storms, zero stray clicks this session.

### How to reproduce

```sh
cd "/private/tmp/claude-501/-Users-tarucy-project-podoui/885735ab-d9d3-4bc8-9008-5a94bf821edb/scratchpad/harness/native"
shasum ../../podo-ui-2.1.0.tgz                       # 1f56298f9cab674dcc8d7c787189674ad315767c
rm -rf node_modules/podo-ui node_modules/.vite package-lock.json && npm install ../../podo-ui-2.1.0.tgz
shasum node_modules/podo-ui/dist/native/index.js     # 1bb65c59abbef56df38c899baf2376ec763c8f03
grep -c defaultSelected node_modules/podo-ui/dist/native/index.js   # 1 (0 in the native8/2.0.4 copy)
sh start.sh                                          # Vite on http://127.0.0.1:5214
./ab9.sh open http://127.0.0.1:5214                  # ab9.sh = logging wrapper: npx agent-browser --session native9
# … drive checks (full command lines + outputs: reports/native-commands.log, session-native9 banner)
(cd /Users/tarucy/project/podoui && npx vitest run packages/native --reporter=verbose)   # 44/44; pnpm check: 23 files / 349 passed | 2 skipped = 351 (check-fixture9.log:73-74)
./ab9.sh close && kill $(cat server.pid)             # teardown (done: "server down confirmed (native9 final)")
```

---
---

# HISTORY (superseded rounds — kept for audit; every claim below is superseded by session native8 above where they differ)

## History: Round 6-spot — session `native7` (2026-07-21, tarball sha1 7064859bf44bc9bacf7b20fecb102d91287f3b0b)

R5 spot-check, 6/6 PASS against the then-final tarball `7064859b…` (dist/native `ee81607838…`): provider `iconGlyphs` map (name→glyph resolution, explicit-prop wins, raw-name fallback), opacity-free Button disabled treatment + `#123BBA` pressed fill captured mid-hold, Select menu box/content-container split + keyboard scroll-follow (12× ArrowDown → scrollTop 0→230), Field counter external-controlled sync (`0/20`→`10/20` with no typing), reworked stale-lock unit test + browser flip regression, and a clean console sweep. Banner `=== R5 SPOT-CHECK session native7 tarball 7064859bf44bc9ba ===`. Repo gate then: 276 passed | 2 skipped (278); focused native run 26/26. Screenshots `j01`–`j05`. That build had NO round-6 contract (aria-haspopup/aria-pressed/tooltip greps 0 — added in the `634f24ad…` build verified above).

## History: Round E+fix — sessions `native5`/`native6` (2026-07-20, tarballs 02fbd90ecc348460d5c8883e14e69af1578f0dcc / 76b395ba4081bf5507d0949dde59a1eac1c1d61f)

- native5: definitive 24-item run, 23/24 PASS against `02fbd90e…` (dist/native `c0dc3745…`, first build with the listbox contract: `aria-activedescendant` ×3 vs 0 in native4). Introduced: the disabled-treatment checks (B1-then), describedBy join (B2-then), Icon aria-hidden (B3-then), listbox contract (B4-then), long-list 474px cap (B6-then), Field OR semantics (B7-then).
- native5's single FAIL (B5-then): RNW's PressResponder counts Enter on ANY element as a key press (`isValidKeyPress`, PressResponder.js:66-72; keydown→`start()` :240-243, keyup→`end()`→`onPress` :209-218), so the Select trigger's `onKeyDown` AND its `onPress` both ran on one Enter — Enter-from-closed netted closed (`cells:0`), Enter-pick reopened (`menuOpenAfterEnter:1`). The repo test passed because its fake host had no press synthesis; real RN has no DOM keyboard — RNW-web-specific. Preserved verbatim in the native5 section of the old report (see git/file history of this document).
- native6: the exact suspected fix shipped (`suppressSynthPress` ref in `packages/native/src/index.tsx` — a handled Enter keydown swallows exactly the next synthesized press; unit test models RNW's synthesis) in tarball `76b395ba…` (only delta vs `02fbd90e…` = dist/native, `fef6cec5…`); the full keyboard + mouse flow re-verified → 24/24. Banner `=== B5 REVERIFICATION session native6 tarball 76b395ba… ===`. Screenshots `h01`–`h14`, `i01`–`i05`.
- Environmental artifacts that round: two about:blank tab resets after `press Enter` on focused checkbox/radio (recovered by reload + full rerun), one click storm on `btn-toast-show` (3514 stray clicks — rerun with in-page counted clicks), and two same-eval read-after-click races (re-read in fresh evals). Repo gate then: 261 passed | 2 skipped (263); native suite 22/22.

## History: Round D — session `native4` (tarball sha1 af9c435ab12272b57b2c188cf711112416faeb31)

16/16 against `af9c435…` (dist/native `f09077df…`). First round with: Chip onPress counter, the Select disabled-flip transition (then newly fixed via `openState && (disabled || readOnly)`), disabled/readOnly Textarea typing attempts, Field disabled/invalid propagation onto prop-less children, Switch/Checkbox/Radio keyboard (Enter-only then — Space no-op), and the 13-exports unit citation. That build's Select had no listbox contract (plain View menu, BUTTON cells, no aria-activedescendant, no ScrollView cap) and no `focusable:false` on disabled TextInputs. Artifacts: one about:blank reset, a click storm on `btn-lock-toggle` (~130–180 clicks/sec), two more resets after checkbox/radio keys.

## History: Round 3 — session `native3` (tarball sha1 b70028087aeba928ef88a2ba01215e1ef03d0825)

14/14. First evidence for: disabled removable Chip, Select accessibilityLabel on the trigger, disabled/readOnly multi-select locking, standalone required (fiber proof + unit test), distinct auto Field ids, disabled-Radio counter. Recorded the RNW accessibilityState-drop honesty notes and the react-batching eval artifact (3 picks in one eval → only last registered). Corrected the round-1 "dark scheme flips Button bg" claim — Buttons keep static `BUTTON_COLORS` in both schemes (re-confirmed through native8 A1).

## History: Round 2 — session `native2` (intermediate repack ec37fab54586fcc0ed5e7b3737091f48c71dc9e1)

Verified fixes F1 (combobox trigger, no nested buttons), F2 (8 button themes + 4 sizes), F3 (invalid danger borders) plus a regression sweep. Screenshots `r1`–`r4`.

## History: Round 1 — baseline (first tarball, session `native`)

First full pass; found F1 (nested `<button>` in the multi-select trigger → React DOM-validity error), F2 (all button themes identical bg), F3 (invalid had no effect) — all fixed and re-proven in every later round, most recently native8 A2/A6/A7/A9/A10 (0 nested pairs, 8 distinct themes, danger borders). Screenshots `01`–`10`.

## Final artifact delta (tarball b691f35c3c93…)

Two findings from the frozen round (session *8) were fixed after it:
1. The default `podo build` now emits the Badge binding vars: the CLI's
   defaultComponentDocuments embeds the spec badge document and the default
   token document gained the `color.palette.*` tokens it references (values ==
   the styles.css fallback hexes). Regression-locked by the CLI build test
   (`--podo-badge-root-background:` / `.podo-badge[data-theme="red"]` /
   `--podo-color-palette-error-5: #FEF1F1` assertions).
2. The Editor's unwired second hidden file input was removed (uploads into it
   were silently inert). Regression-locked by the editor test
   ("renders exactly one file input, wired to the image upload flow").
Repo gate on the final artifact: 23 files / 327 passed | 2 skipped (329).
Micro-verification in the react harness (session microv, logged commands):
fresh install + `podo build --force` → components.css contains 33
`--podo-badge-*` declarations (e.g. `[data-theme="red"] {
--podo-badge-dot-color: var(--podo-color-palette-accent-50); … }`), tokens.css
defines the palette vars; in-browser, the danger Badge's
`--podo-badge-root-background` computes to `#F23B3B` with backgroundColor
rgb(242,59,59) (previously the var computed empty), and each of the 3 mounted
Editors renders exactly one `input[type=file]` (was two each). No other module
changed: only dist/cli and dist/react/editor differ from 634f24ad.

## Closing delta (final tarball 9da8f7ec6667…)

Nine last real bugs from adversarial round 6 were fixed with red→green tests
(repo gate: 23 files / 340 passed | 2 skipped = 342): uncontrolled DatePicker
committed values now render (+`defaultValue` prop; browser-verified in the
react harness: placeholder → "2026 - 07 - 15" after an uncontrolled pick);
Select chip-remove/clear-all keyboard activation no longer swallowed by the
trigger handler; controlled/forced-open Tooltips re-measure on scroll/resize;
re-clicking a selected Editor image keeps a single wrapper; SSR aria-expanded/
aria-controls now track the portal menu's actual presence; native Select
clear-all routes through the stale-event lock; labeled dot Badges are real
accessibility elements (native accessible+image role, hono role="img");
hono Tooltip accepts an id for the spec's aria-describedby contract.
Remaining adversarial-round-6 items are NOT bugs and are catalogued as:
feature backlog (native uncontrolled-state APIs, Field label-press focus,
DatePicker enable/disable-function browser fixtures, Editor toolbar-filter/
resize fixtures, Tooltip position fixtures), design questions (Typography
variant default derived from `as` — consistent react/hono behavior), and
environment-bound evidence (real iOS/Android assistive-technology behavior —
RNW + unit tests are the attainable proxy here).
