# Podo UI v2 — React runtime FROZEN-ARTIFACT FINAL verification report (round 6 / session react8)

- **Session: `react8` — this document is the single source of truth.** Every claim below cites output produced in THIS session; log pointers are line numbers in `reports/react-commands.log` (react8 banner `=== FROZEN FINAL VERIFICATION session react8 tarball 634f24adaa889b613dbc1fee9e8f77c72ddf3d3e ===` at **log:8810**). Everything from earlier rounds lives only in the History appendix.
- Date: 2026-07-21.
- **Artifact under test:** `podo-ui-2.0.4.tgz`, **`shasum` = `634f24adaa889b613dbc1fee9e8f77c72ddf3d3e`** (file dated Jul 21 12:11; shasum quoted at log:8811-8812 immediately after the banner). The repo is code-frozen; this tarball is exactly what the adversarial reviewer judges.
- **Clean reinstall** (log:8815-8823): `rm -rf node_modules/podo-ui package-lock.json && npm install ../../podo-ui-2.0.4.tgz` → `added 1 package … found 0 vulnerabilities`; installed `package.json` version readout `2.0.4`.
- **Bit-for-bit freshness proof** (log:8826-8841): four hash pairs byte-identical between the installed package and a fresh extract of the 634f24ad tarball (`tarball-inspect-react8/`):
  - `dist/react/index.js` = `06c2e00766f61be352a404469518d691fe749dae` — **build-unique this round** (react7's tarball had `e7d333f3…`; carries the new Icon size/decorative rendering and the B4 callback pass-throughs),
  - `styles.css` = `fa65c26a969aa0b521d9b6c2159a02d0c27a05b7` — **build-unique this round** (react7 had `55d4f4e3…`; carries the new `.podo-icon[data-size]` 16/24/32px rules),
  - `dist/react/editor/index.js` = `f5d2c9d8d6c1c55623aacb0f1cce34b8bb9ea0d5`,
  - `dist/react/datepicker.js` = `a2d8fd629f7552d63e9ef6577d05f8ee9a3c6f2d` (unchanged from react7 — no datepicker fix landed this round).
- Repo state: READ ONLY (`git status` untouched by this session). `pnpm check` green — **`Test Files  23 passed (23)` / `Tests  326 passed | 2 skipped (328)`** (quoted from `check-final.log`, timestamped Jul 21 12:11 — same minute as the tarball).
- **Harness changes this round** (all inside `harness/react`, repo untouched):
  1. `src/App.tsx` — new section `14b. B4` (Switch `onClick`+`onCheckedChange`, Checkbox native `onChange`, two Radios with `onCheckedChange`, Input/Textarea native `onChange`, each appending to a visible readout log); `onOptionAdd={(o) => setB4AddPayload(JSON.stringify(o))}` added to the existing addable Select with a `sel-add-payload` readout; five B1 Icon fixtures (`size` sm/md/lg, default, `decorative={false} aria-label="검색"`). Typecheck proof: repo-devtools `tsc -p tsconfig.check.json` → `tsc exit 0` (log:8905-8924).
  2. **B3 pipeline setup** (log:8844-8903): the default `podo build` output has **NO** `--podo-badge-*` definitions (`grep count: 0` quoted at log:8845-8847). Copied `packages/spec/samples/components/badge.component.json` (the Figma-plugin `podo import` payload shape) into `.podo/components/` — first rebuild FAILED with 33 `references missing token "color.palette.*"` errors (validation working, quoted log:8849-8884) — then copied `spec/samples/tokens/{base-palette,color}.tokens.json` into `.podo/tokens/` → `npx podo build --force` → `Generated 17 files`, and `src/podo/components.css` now defines `.podo-badge { --podo-badge-label-color: var(--podo-color-palette-gray-5); --podo-badge-root-background: var(--podo-color-palette-gray-70); }` plus 12 per-theme `.podo-badge[data-theme=…]` blocks (quoted log:8887-8903). Palette values equal the styles.css fallbacks, so all pre-existing computed-style expectations stayed valid.
- Browser: every step ran as `npx agent-browser --session react8 …` through `ab8.sh`, which prints the FULL shell-quoted command line (entire eval JS included) into `reports/react-commands.log` before executing. Viewport 1440×900. Screenshots **m01–m41** in `reports/react-shots/`.
- Teardown: `agent-browser --session react8 close` → `✓ Browser closed`; `kill $(cat server.pid)` → `port5211=down` (end of log).
- **Result: 28 / 28 checklist items PASS** (24 re-run A-items with all prior audit items folded in + new B1–B4). Zero page errors across the whole session; console only ever contained Vite debug + React DevTools info lines.

## How to reproduce

```sh
cd <scratchpad>/harness/react
shasum ../../podo-ui-2.0.4.tgz              # must be 634f24adaa889b613dbc1fee9e8f77c72ddf3d3e
rm -rf node_modules/podo-ui package-lock.json
npm install ../../podo-ui-2.0.4.tgz
# B3 pipeline (simulates the Figma-plugin `podo import` payload; repo read-only):
cp <repo>/packages/spec/samples/components/badge.component.json .podo/components/
cp <repo>/packages/spec/samples/tokens/{base-palette,color}.tokens.json .podo/tokens/
npx podo build --force                      # Generated 17 files in src/podo
./start.sh                                  # vite on http://127.0.0.1:5211/
npx agent-browser --session react8 open http://127.0.0.1:5211/
# ... commands exactly as logged (full text) in reports/react-commands.log (react8 banner at line 8810) ...
kill $(cat server.pid)
npx agent-browser --session react8 close
```

## A. Re-run of the established 24-item checklist (all PASS)

Evidence quotes are verbatim from `reports/react-commands.log`, session react8; pointer = log line of the section banner.

| # | Item | Result | Evidence (quoted from react-commands.log, session react8) |
|---|------|--------|-----------------------------------------------------------|
| 1 | Button (press+keyboard+disabled+variants) | **PASS** | log:8938 — two `.click()` → `"clicks":"2"` (readout flushes next eval — React 19 batching); focus + `press Enter` + `press Space` → `"clicks":"4"`; disabled (`{"disabled":true,"ariaDisabled":"true"}`) REAL mouse click at measured (375,364) → `"clicks":"4"` unchanged; variants `dangerSm {"theme":"solid-danger","size":"sm","bg":"rgb(242, 59, 59)","h":36}`, `outlineLg {"border":"rgb(66, 108, 237)","h":52}`, `assistiveXs {"bg":"rgb(244, 244, 245)","h":32}`, `iconChildren:2`. Shot m02 |
| 2 | Badge computed styles | **PASS** | log:8965 — DOM quote `<span data-testid="badge-dot" class="podo-badge" data-theme="danger" data-dot="true"></span>` (`dotText:""`, `dotW:6,dotH:6`); computed `natural {bg rgb(62,66,75), color rgb(249,249,249)}` vs `danger {bg rgb(242,59,59)}` vs `red {bg rgb(254,241,241), color rgb(242,59,59)}` vs `purple {bg rgb(248,245,255), color rgb(142,81,255)}`; count badge `{"text":"7","theme":"info","bg":"rgb(0, 149, 255)"}`. Shot m03 |
| 3 | Chip (controlled/removable/disabled/onPress) | **PASS** | log:8982 — toggle `{"ariaPressed":"true","state":"true"}` → `"false"/"false"`; controlled click → `{"ariaPressed":"false","ctlState":"false"}` (prop wins) → `chip-ctl-set` → `{"ariaPressed":"true","ctlState":"true"}`; `{"pressCount":"2"}` after 2 clicks; removable X → `{"removed":"true","chipInDom":false}`; disabled removable `{"dataDisabled":"true","tag":"SPAN","removeDisabled":true,"removeAriaDisabled":"true","removeTabIndex":-1}` + REAL click (976,521) + synthetic `.click()` → `disRemoved "false"` both reads. Shot m04 |
| 4 | Switch (+disabled) | **PASS** | log:9021 — click → `{"ariaChecked":"true","state":"true"}` → `"false"`; disabled switch REAL click at (364,831) → `{"switchState":"false","ariaChecked":"false"}` |
| 5 | Checkbox (mixed, +disabled) | **PASS** | log:9021 section — cb-main click → `{"cbState":"true","checked":true}` → back to `"false"`; disabled cb REAL click (346,971) → `cbState "false"`; indeterminate `{"indeterminateDOM":true,"dataState":"indeterminate"}`; a11y snapshot line `checkbox "부분 선택" [checked=mixed, ref=e123]`. Shot m05 |
| 6 | Radio (+disabled) | **PASS** | log:9021 section — a→c → `{"picked":"peach","a":false,"c":true}` → a again → `"apple"`; disabled radio-d REAL click (435,1111) → `{"picked":"apple","d":false}` |
| 7 | Input (controlled/disabled/readOnly/invalid) | **PASS** | log:10748 — controlled `{"domValue":"포도 주스","stateReadout":"포도 주스"}`; uncontrolled lg `"12000"`; disabled: REAL click (424,538) + `keyboard type '차단됨'` → `{"disValue":"수정 불가","activeElement":"BODY"}`; readOnly `{"attr":true,"wrapState":"read-only","borderColor":"rgba(0, 0, 0, 0)"}`; invalid `{"ariaInvalid":"true","wrapState":"invalid","borderColor":"rgb(242, 59, 59)"}`; `normalWrapBorder "rgb(228, 228, 231)"`. Shot m07 |
| 8 | Textarea (countMax/disabled/readOnly) | **PASS** | log:10773 — `taReadout "여러 줄 입력 테스트"`; countMax=20: 19 real chars + 6 more attempted → `{"valueLen":20,"maxLength":20,"counter":"20/20"}`; disabled REAL click (424,686) + type → `{"disValue":"비활성 텍스트영역","active":"BODY"}`; readOnly `{"dataState":"read-only","readOnly":true,"borderColor":"rgba(0, 0, 0, 0)"}`. Shot m08 |
| 9 | Select single (keyboard/controlled/disabled) | **PASS** | log:10802 — options `["사과","포도","복숭아","멜론","딸기"]` → pick → `{"readout":"grape","trigger":"포도","menusOpen":0}`; ArrowDown×4 → `{"activedescendant":"_r_1_-3","activeCellText":"멜론"}` → Enter → `"melon"`; outside click `1→0`; controlled: pick 포도 → `{"ctlTrigger":"사과","ctlState":"apple","ctlCb":"grape"}` (prop wins) → external set → `{"ctlTrigger":"멜론","ctlState":"melon"}`; disabled `{"dataState":"disabled","ariaDisabled":"true","tabIndex":-1}` + click → `0` menus. Shot m09 |
| 10 | Select multi (chips/maxChips/clear/controlled/readOnly/disabled) | **PASS** | log:10855 — 3 picks (separate evals) → `{"readout":"apple,grape,berry","checkedOptions":3}`; chips `["사과","포도"]` + `{"moreText":"+1","moreClass":"podo-select__chip-more"}`; chip X 포도 → `"apple,berry"`; clear (`aria-label "모두 해제"`) → `{"readout":"(none)","chips":0}`; controlled: caret-area REAL click (419,450) → `{"expanded":"true"}`, pick 멜론 → `{"chips":["포도","딸기"],"state":"grape,berry","cb":"grape,berry,melon"}` (prop wins), external set → `{"chips":["포도","딸기"],"state":"grape,berry"}`; disabled `{"rootClass":"podo-select","dataState":"disabled","chips":[{"label":"사과","dataDisabled":"true","hasRemoveButton":false},…],"clearButton":false}` + click → `0`; readOnly `{"dataState":"read-only","ariaReadonly":"true","chips":[{"label":"복숭아","hasRemoveButton":false},…],"borderColor":"rgba(0, 0, 0, 0)"}` + click → `0`. Shot m10 |
| 11 | Select defaultOpen guard | **PASS** | log:10936 — fresh reload at `scrollY 0`, `errors` empty: `{"plainDefaultOpen":{"dataOpen":"true","ariaExpanded":"true","menuInDom":true},"doDisabled":{"ariaExpanded":"false","menuInDom":false},"doReadonly":{"ariaExpanded":"false","menuInDom":false},"listboxesOnPage":1}`; root attribute dumps quoted: `data-state=disabled` / `data-state=read-only` on `.podo-select` with `aria-disabled=true tabindex=-1` / `aria-readonly=true`; clicking both inert triggers → still `aria-expanded "false"` each, 1 listbox total (the plain one). Shot m11 |
| 12 | Select searchable + focus restore | **PASS** | log:10950 — REAL click opens with FOCUS ON the search input: `{"activeTag":"INPUT","activeClass":"podo-select__search","role":"combobox","ariaExpanded":"true","ariaControls":"_r_8_","ariaAutocomplete":"list"}`; type `포` → `{"visibleOptions":["포도"]}`; ArrowDown → `{"ariaActivedescendant":"_r_8_-0","activeCellText":"포도"}`; Enter → `{"picked":"grape","menuStillOpen":false,"activeClass":"podo-select__trigger","activeElementIsStoredTrigger":true}` (identity pinned via `window.__selSearchTrigger`); PATH B: reopen via Enter (focus lands on search input), type `멜`, Escape → `{"menuStillOpen":false,"activeElementIsStoredTrigger":true,"value":"grape"}`. Shot m12 |
| 13 | Select addable outside listbox | **PASS** | log:10984 — `{"listboxChildRoles":["option"×5],"addRowInsideListbox":false}`; DOM quote `<div class="podo-select__menu"><div class="podo-select__add"><input class="podo-select__add-input" placeholder="새 과일 입력"…><button … class="podo-select__add-button">추가</button></div><div class="podo-select__listbox" role="listbox" id="_r_9_">…`; add `두리안` → `{"readout":"두리안","trigger":"두리안"}` (+ the B4 payload, below); reopen → `{"count":6,"sixth":{"t":"두리안","sel":"true"}}`. Shot m13 |
| 14 | Select a11y routing | **PASS** | log:11007 — label DOM `<label class="podo-field__label" id="_r_e_-label" for="fruit-select-a11y">과일 필드 라벨</label>`; `{"labelHtmlFor":"fruit-select-a11y","comboboxId":"fruit-select-a11y","comboboxTag":"DIV","comboboxClass":"podo-select__trigger","comboboxRole":"combobox","match":true}` |
| 15 | Select viewport flip | **PASS** | log:11010 — `{"spaceBelow":60}` → open → `{"menuTop":546,"menuBottom":790,"trigTop":796,"flippedAbove":true,"fullyInViewport":true,"optCount":5}` → pick while flipped → `{"readout":"berry","menusOpen":0}`. Shot m14 |
| 16 | Tooltip (forced/hover/focus/controlled/inline) | **PASS** | log:11023 — forced `{"tooltipsVisibleNow":["항상 열린 도움말"],"forcedOpenPresent":true}` (note: `position:fixed` tooltips have null `offsetParent` — visibility read via rect width); hover → `{"text":"위쪽 도움말","posAttr":"top","tooltipAboveAnchor":true}`; unhover → only forced remains; focus → `{"focusOpened":true,"pos":"right","theme":"reverse","rightOfAnchor":true}`; controlled toggle → `["제어형 도움말"]` → `[]`; inline (REAL hover, `portal={false}`) → `{"inlineTipInsideWrap":true,"text":"인라인 도움말","position":"static"}`. Shot m15 |
| 17 | Toast (states/auto/manual/pause/dismiss/custom max-duration-position) | **PASS** | log:11075 — five states in one in-page sampler: at60 all five (`normal/success/danger/info/warning`, danger `role:"alert"` + caption, others `role:"status"`), at900 max=3 kept `warning/info/danger`; in-page 4.2s → `remaining [{"state":"danger","text":"실패했습니다다시 시도하세요"}]` (manual persists) → close X (`aria-label 닫기`) → `0`; pause-on-hover: pointer PRE-PARKED at (737,42), toast fired under it, in-page 8s (duration 3000) → `{"stillPresent":true,"state":"success"}`, REAL unhover + 4.2s → `0`; DEFAULT burst `{"at60":["burst 4","burst 3","burst 2","burst 1"],"at700":["burst 4","burst 3","burst 2"],"afterDismissAll":[]}`; CUSTOM (`{"toasterMode":"custom","position":"bottom-left","left":16,"bottom":884,"winH":900}`): burst at700 `["burst 4","burst 3"]` (max=2; burst toasts carry their own `duration:15000`), un-optioned toast 100ms-poll → **`customDurationMs:1701`** (≈1500+exit, well under default 3000). Shots m16, m17 |
| 18 | Table (select-all/toggle/disabled/defaultSelected-filtered/drag) | **PASS** | log:11134 — defaultSelected `"3"`, row states quoted (row2 `{"disabled":"true","cbDisabled":true}`); select-all → `{"readout":"0,1,3","headerState":{"checked":true}}` → again → `"(none)"`; row-body click → `"1"`; disabled-row click → `"1"`; REAL drag r0(294,394)→r1→r2→r3(294,545): mid-drag `"0,1"`, release → `{"readout":"0,1,3","rowSelected":["true","true",null,"true"]}`; `table-disdefault` (defaultSelected [1,2], row2 disabled): row1 `{dataSelected:"true", cbChecked:true}`, **row2 `{dataDisabled:"true", dataSelected:null, cbChecked:false, cbDisabled:true}`** — disabled index filtered. Shot m18 |
| 19 | Field (wiring/disabled-OR/invalid-OR/counter-controlled/explicit-id) | **PASS** | log:11224 — generated `{"generatedFor":"_r_l_-control","inputId":"_r_l_-control","forMatchesInput":true,"requiredMark":true,"subLabel":"선택","helper":"회사 이메일을 입력하세요","helperId":"_r_l_-description","inputDescribedby":"_r_l_-description"}`; error `{"errorText":"8자 이상이어야 합니다","errorId":"_r_m_-error","inputAriaInvalid":"true","describedby":"_r_m_-error"}`; explicit `{"labelFor":"member-email-input","childInputId":"member-email-input","match":true}`; Field-disabled REAL click (424,652)+type → `{"value":"","active":"BODY"}`; OR-disabled (`<Field disabled><Input disabled={false}/>`) → `{"inputDisabledAttr":true,"wrapDataState":"disabled","fieldDataDisabled":"true"}`; OR-invalid → `{"wrapDataState":"invalid","inputAriaInvalid":"true","ariaInvalidCountInField":1,"borderColor":"rgb(242, 59, 59)"}`; live counter 5 real chars → `{"value":"다섯글자입","counter":"5/30"}`; external controlled set (no typing) → `{"counter":"9/30","inputValue":"외부에서 넣은 값","valueLength":9}`. Shot m20 |
| 20 | Icons E001–E009 | **PASS** | log:11265 — all 9 via `::before`: `menu U+E001, chevron-left U+E002, chevron-right U+E003, calendar U+E004, time U+E005, refresh U+E006, check U+E007, close U+E008, search U+E009`, each `fontFamily:"PodoIcons", nonEmpty:true`. Shot m21 |
| 21 | Typography responsive + theme | **PASS** | log:11274 — `{"viewportW":1440,"h1":{"cls":"podo-text podo-text--h1","fontSize":"32px","fontWeight":"700"}}` → viewport 900 → `"28px"` → restore → `"32px"`; toggles: provider attrs quoted `["data-podo-theme=dashboard","data-color-scheme=dark"]` with `{"stateReadout":"dashboard/dark","contextReadout":"dashboard/dark"}` → restored `"landing/light"` both. Shots m22, m23 |
| 22 | DatePicker exhaustive | **PASS** | 12 combos (log:11297): 1 `"2026-07-10"` (instant, closed); 2 pending `"(none)"` → 적용 → `"2026-07-12"`; 3 `"14:00"` → `"14:30"`; 4 → `"9:00"`; 5 `"2026-07-05 8:15"`; 6 → `"2026-07-06 18:00"`; 7 `"2026-07-03"` → `"2026-07-03 ~ 2026-07-21"` (`closedOnCompletion:true`); 8 → pending cells `["2026년 7월 2일","2026년 7월 9일"]` → `"2026-07-02 ~ 2026-07-09"` (clean rerun after a same-eval double-click collapse — React 19 batching artifact, noted in log; recovered via the picker's own 초기화); 9 `"10:30 ~t 12:00"`; 10 → `"7:00 ~t 8:30"`; 11 `"2026-07-01 6:00 ~ 2026-07-25 ~t 20:00"`; 12 → `"2026-07-04 5:00 ~ 2026-07-11 ~t 22:00"`. **Edges** (log:11482/11509/11531): placeholder `{"placeholderText":"날짜를 선택하세요","hasPlaceholderClass":true}`; outside-click cancel: pending `trigger "2026 - 07 - 25"` + REAL outside click (120,300) → `{"readout":"2026-07-12","trigger":"2026 - 07 - 12","dropdownOpen":false}`; 초기화 → `{"readout":"(none)","trigger":"YYYY - MM - DD"}` (cleared value COMMITTED); Escape → `{"dropdownOpen":false,"focusReturnedToTrigger":true,"activeClass":"podo-dp-inputPart "}` (no tab hiccup this round); disabled-at-mount `{"dateBtn":{"disabled":true,"ariaDisabled":"true"},"timeSelects":[{"시 선택",disabled},{"분 선택",disabled}]}` + REAL click (327,450) → closed, readout `"(none)"`; **minuteStep boundary via REAL UI**: options `["0","30"]`, committed `10:00`, picked minDate day 7/15 (min 10:59) → **`"2026-07-15 11:00"`** (step-ceil carry clamped, never 10:60); **boundary 23:59** `{"minuteValue":"59","minuteOptions":[0 disabled, 30 disabled, 59 selected],"hourValue":"23","hoursDisabled":23}` with committed readout `"2026-07-15 23:59"`; **keyboard grid** (log:11550): `{"cellsWithTabindex0":1,"rovingCellLabel":"2026년 7월 21일"}` (today), Tab leaves grid, ArrowRight → 7/22, ArrowDown → 7/29, ArrowDown crosses month → `{"focusedDay":"2026년 8월 5일","gridLabel":"2026년 8월"}`; Enter-commit proven on the wired picker: roving = selected 7/10 → ArrowRight 7/11 → Enter → `{"readout":"2026-07-11","trigger":"2026 - 07 - 11","dropdownOpen":false}`. **Folded audit items:** disabled-flip-while-open (log:11597): pending 7/15 + flip → `{"disabledReadout":"true","dropdownOpen":false,"applyButtonAnywhere":false,"triggerText":"2026 - 07 - 10","triggerDisabled":true,"readout":"2026-07-10","commits":"0"}` — cancel semantics, zero commits; disabled trigger inert; re-enable stays closed (DOM quote `aria-disabled="false"`, `commits:"0"`); constrained keyboard (log:11622): roving SKIPS disabled selected day (`{"tabbableCount":1,"rovingLabel":"2020년 1월 2일","selectedCell":{"label":"2020년 1월 10일","disabled":true,"tabindex":"-1","ariaSelected":"true"}}`), ArrowRight skips (`{"nowFocused":"2020년 1월 12일","skippedCellTabindex":"-1"}`), fully-disabled month `{"cellCount":35,"disabledCount":35,"tabbableCount":0}`; Home/End (log:11647): `Home` → `"2026년 7월 7일"` (skipped disabled Sun 7/5 + Mon 7/6), `End` → `"2026년 7월 10일"` (skipped Sat 7/11); yearRange/maxDate (log:11647): `{"selectedYear":"1990","selectedText":"1990년","optCount":32,"firstFive":["1990년","2000년",…],"lastTwo":["2029년","2030년"]}` → year 2030 + 12월 → 다음 달 → `{"gridLabel":"2031년 1월","yearSelectValue":"2031","has2031":true,"firstOpt":"2000년"}` (1990 extra option gone once not the view year); period year clamp (log:11696): defense-in-depth quoted first — at left year 2026 the month select offers ONLY `["1월"…"8월"]`; real clamp: left Dec 2025 → year 2026 → `{"gridLabels":["2026년 8월","2026년 8월"],"leftMonthSelected":"8월","rightGridUnchanged":true,"leftEqualsRightBoundary":true}`; reverse-order datetime period (log:11696): preset `"10:00 ~t 18:30"`, clicked LATER 7/20 FIRST then EARLIER 7/10, 적용 → **`"2026-07-10 18:30 ~ 2026-07-20 ~t 10:00"`** — dates auto-sorted AND both times kept, swapped along; controlled cross-month (log:11765): external set → `{"readout":"2026-11-05","trigger":"2026 - 11 - 05"}`, reopen → `{"gridLabel":"2026년 11월","selectedCell":"2026년 11월 5일"}`. Shots m24–m33 |
| 23 | Editor (13 groups + image edit/delete + table ctx menu + youtube + code view/validator + A→B→A + ids + height + scope) | **PASS** | Inventory (log:11780): `toolbarGroupCount:13`, all 19 titles quoted (실행 취소 … HTML 코드보기); a11y `{"role":"textbox","ariaMultiline":"true","ariaLabel":"리치 텍스트 편집기","contenteditable":"true"}`; EditorView `<div class="podo-ed-editorView "><p>뷰어 <strong>강조</strong> 텍스트</p></div>`. (1) undo/redo (log:11785): typed `안녕하세요 포도` → undo `"(empty)"` → redo restored. (2) 제목 1 → `<h1>…</h1>`. (3) bold on h1 → `<span style="font-weight: normal;">`, then `<i><u><strike>`. (4) **cross-block color** (log:11818): retyped `제목줄`⏎`본문줄` → `<p>제목줄</p><p>본문줄</p>` → 제목 1 → REAL mouse drag selection spanning both blocks (`selText:"제목줄\n본문줄", anchorIn:"p", focusIn:"h1"`), `글자색 #ff0000` → `<h1><span style="color: rgb(255, 0, 0) !important;">제목줄</span></h1><p><span …>본문줄</span></p>` with `spanInsideH1/spanInsideP:true, spanWrappingBlock:false`; re-selected the SAME run by real drag, `글자색 #0000ff` → both spans restyled in place: `spanCount:2, nestedSpan:false`. (5) 배경 색상 → `<span style="background-color: #ffff00 !important;">`; 가운데 정렬 → `<h1 style="text-align: center;">`. (6) 목록 → `</li></ul>`, then (caret re-placed) 번호 목록 → `</li></ol>` (log:11931). (7) 구분선 → `…본문줄</span><hr></li>` (log:11952). (8) 링크 → `<a href="https://podo.example.dev" target="_blank" rel="noopener noreferrer">본문줄</a>`. (9) **table** (log:11967): grid selector cells are `div[role=button]` (e.g. `aria-label="2×2 표 삽입"` — DOM quoted); insert → `{rows:2, cellsPerRow:[2,2], activeInEditable:true}`, `<table border="1" style="border-collapse: collapse; width: 100%; …">`; REAL right-click on td → `.podo-ed-tableContextMenu` (all 12 items quoted) → 아래에 행 추가 → `{rows:3, readoutRowCount:3, activeElement:{tag:"DIV", cls:"podo-ed-editorContent"}, menuClosed:true}` → 열 삭제 → `{cellsPerRow:[1,1,1], readoutTdCount:3, activeInEditable:true, menuClosed:true}`. (10) **image** (log:12032): REAL `upload` of tiny-test.png into the dialog's WIRED hidden input (the editor renders TWO hidden file inputs; only the second has `onChange` — see artifacts §5) → `{fileNameShown:"tiny-test.png", previewImg:true, previewIsData:true}` → 삽입 → `{imgInserted:true, isDataUri:true, alignWrapperStyle:"text-align: left;"}`; REAL click on the 1×1px img at (278,449) (`elementFromPoint` = IMG verified) → edit popup: `{selectionWrapper:true(.image-wrapper), popupActionButtons:["이미지 삭제","취소","적용"], sizeButtonsVisible:4}` → 이미지 삭제 → `{imgRemains:false, wrapperRemains:false, emptyAlignmentDivCount:0}`. (11) youtube (log:12103) → `{iframeInserted:true, src:"https://www.youtube.com/embed/dQw4w9WgXcQ", embedUrlOk:true, width:"100%"}`. (12) 서식 지우기: select-all → all inline styles stripped (readout quoted; also collapses block wrappers — noted §6). (13) **code view** (log:12112): textarea `ariaLabel "리치 텍스트 편집기"`, toggle button title flips to `에디터로 전환`; typed `<p>코드뷰에서 입력</p>` → readout LIVE `containsTyped:true`; toggle back → `editableContainsTyped:true`. **Editor2 REAL zod validator** (log:12127): `z.string().superRefine` passed straight to `validator`; typing fired it per edit (`valCalls 0→10→16`); race sampler (50ms): `검증 성공` at t → error `금지어는 사용할 수 없습니다` **40ms later** (inside the 2s window) → 3.5s later still showing, `msPastStaleWindow:17694`, `fullValLog` shows no clearing transition; **A→B→A**: emitted-from-code-view `taValue "<p>AAA</p>"` → external B → `"<p>BBB\n</p>"` → external A (identical string) → **`"<p>AAA\n</p>"`** (no echo-swallow); external set WHILE code view open → `{taValue:"<p>외부에서 갱신된 본문\n</p>", html2:"<p>외부에서 갱신된 본문</p>"}`. **Two/three editors** (log:12164): `{"count":3, ids:[podo-editor-_r_s_ (main), podo-editor-_r_t_ (second), podo-editor-_r_u_ (third)], distinct:true, nonePlain:true}`; independence: marker typed in editor1 → `{html1ContainsMarker:true, html2ContainsMarker:false}`. **Height**: editor3 `{inlineHeight:"120px", computedHeight:"120px", boundingHeight:120}` vs editor1 control `220px`. **Ctrl+Z scope**: typed `바깥입력` in the OUTSIDE input, `press Meta+z` → `{editorUnchanged:true, activeElement:"outside-input", outsideValue:"바깥입력"}`. Shots m34–m40 |
| 24 | Final console/errors sweep | **PASS** | log:12199 — `reload` → 22 sections; readouts intact (`dpBoundary "2026-07-15 23:59"`, `dpYrTrigger "1990 - 06 - 15"`, `tableSelected "3"`, editor3 present, B4 fixtures + labeled icon present, `badgeVar "#F23B3B"` still resolved from components.css); `errors` → **EMPTY output** (zero page errors); `console` → only `[debug] [vite] connecting…/connected.` and `[info] %cDownload the React DevTools…` lines. Shot m41 |

## B. New round-6 checks (all PASS)

| # | Item | Result | Evidence |
|---|------|--------|----------|
| B1 | Icon spec: size CSS + decorative/labelled a11y | **PASS** | log:11265 — DOM + computed styles quoted for all five fixtures: `<i … data-size="sm" class="podo-icon podo-icon-search"></i>` → **`fontSize:"16px"`**; md → **`"24px"`**; lg → **`"32px"`** (the new `.podo-icon[data-size]` rules in this round's styles.css, fa65c26a…); default `<i … aria-hidden="true" data-size="md" …>` → `fontSize:"24px"`, `role:null`; `<Icon decorative={false} aria-label="검색">` → **`<i aria-label="검색" role="img" data-size="md" class="podo-icon podo-icon-search"></i>`** — `role:"img"`, `ariaLabel:"검색"`, **`ariaHidden:null`** (NO aria-hidden). Shot m21 |
| B2 | Table drag released OUTSIDE the table | **PASS** | log:11175 — clean run: selection reset to `"(none)"`; release point (120,650) pre-verified as a plain page `DIV` (an earlier attempt released over the second table's TD — noted, full check re-ran cleanly); REAL drag r0(294,394)→r1(294,444)→(120,650)→`mouse up` OVER THE PAGE BODY → `readoutAfterOutsideRelease:"0,1"`; then REAL click on normal row3 body cell (603,545) → **`{"readout":"0,1,3","row3Selected":"true"}`** — the click was NOT swallowed (the pre-fix behavior this guards against). Shot m19 |
| B3 | Badge token bindings (components.css vars + computed binding + override) | **PASS (with pipeline caveat)** | Setup quoted in the log (log:8844-8903): **a default `podo build` emits NO `--podo-badge-*` definitions** (`grep count: 0`); after importing `badge.component.json` + the palette token samples (the Figma-plugin `podo import` payload; tarball itself ships neither), `podo build --force` emits `.podo-badge { --podo-badge-label-color: var(--podo-color-palette-gray-5); --podo-badge-root-background: var(--podo-color-palette-gray-70); }` + 12 per-theme blocks (quoted). Browser (log:8965): danger badge `getPropertyValue("--podo-badge-root-background")` = **`#F23B3B`** (label var `#F9F9F9`); probe element with `background: var(--podo-badge-root-background)` resolves to `rgb(242, 59, 59)` **=== the badge's computed background** (`equal:true`); the exact rule text `--podo-badge-root-background: var(--podo-color-palette-error-50)` found in a loaded stylesheet (`componentsCssRulePresent:true`). **Override demo**: injected style tag `.podo-badge[data-theme="success"][data-testid=badge-success] { --podo-badge-root-background: rgb(10, 120, 210); }` → computed bg `rgb(62, 168, 86)` → **`rgb(10, 120, 210)`** (`changed:true`, danger badge unaffected); note a first attempt with a bare `[data-testid=…]` selector (specificity 0-1-0) did NOT win against the generated per-theme rule (0-2-0) — override selectors must out-rank the generated layer (quoted in log). Shot m03 |
| B4 | Reviewer-flagged callback props | **PASS** | onOptionAdd (log:10984): adding `두리안` through the add row → readout **`addPayload:{"value":"두리안","label":"두리안"}`** (full `SelectOption` object, `JSON.stringify`-ed by the harness). Switch onClick (log:10707): REAL click → `{swClicks:"1", swCc:"true", ariaChecked:"true"}` → second click → `{swClicks:"2", swCc:"false"}` (onClick AND onCheckedChange both fire). Checkbox native onChange: REAL clicks → `cbLog "change:true"` → `"change:true | change:false"` (event.type + target.checked quoted). Radio onCheckedChange: pick A → `"apple:true"`; switch to B → `"apple:true | grape:true"` with `{aChecked:false, bChecked:true}` (fires with `true` for the newly-selected radio; the deselected radio's callback is not invoked with false). Input native onChange: real keystrokes `포도` → `inputLog "change:포 | change:포도"` (one synthetic change per keystroke, value flowing); Textarea native onChange: `줄1` → `taLog "change:줄 | change:줄1"`. Shots m06, m13 |

## Console / page errors

- `agent-browser --session react8 errors` ran at initial load (log:8926 section), after the A11 fresh reload (log:10936), mid-Tooltip (log:11023), after the Escape check (log:11509 section), during the editor upload triage, and as the final sweep (log:12199) — **empty every time** (zero page errors, including with the imported badge component document, three mounted editors, and the zod validator active).
- `console` contains only `[debug] [vite] connecting…/connected.` and `[info] %cDownload the React DevTools…` per page load. No warnings or errors from Podo UI components at any point in the session.

## Environmental artifacts (tool/harness quirks — none are app defects; every affected check PASSed on a clean run)

1. **React 19 same-eval batching**: two period-day `.click()`s issued inside ONE eval collapsed into a single pick (combo 8, log around 11297); state readouts in the same eval as a `.click()` can read pre-flush DOM. All multi-click flows were re-run one-click-per-eval; readouts moved to follow-up evals (Apply commits read after an in-page ≥150ms wait).
2. **agent-browser coordinates are viewport-relative**: a `click h1` auto-scroll invalidated previously measured coordinates once (controlled-multi caret click landed off-screen at y=2525) — re-measured after `scrollintoview` and re-ran.
3. **Toast timing vs CLI latency**: the first pause-on-hover attempt failed because >3s of wall-clock CLI latency elapsed before the pointer arrived; the clean run PRE-PARKS the pointer at the toast's future position, fires the toast under it, and samples in-page. All eviction/duration assertions are in-page samplers (60/700/900ms, 100ms polls). Burst toasts carry their own `duration:15000`, so the custom Toaster's `duration:1500` was measured with an un-optioned toast (`1701ms`).
4. **`offsetParent` is null for `position:fixed` tooltips** — visibility checks use bounding-rect width instead (log:11023).
5. **Editor renders TWO hidden `input[type=file]`s**; only the second (`imageFileInputRef`) has the React `onChange`. `upload` into the first (unwired) input attaches files but nothing happens — no error, silently inert. The check passed via the wired input; **suggested hardening**: drop or wire the first input (`packages/react/src/editor/index.tsx`, refs `fileInputRef` vs `imageFileInputRef`).
6. **서식 지우기 on a multi-block selection** strips inline styles but also collapses the block wrappers (h1/p merged into bare text, log:11818 section); the editor recovers on retype. Cosmetic scope note, matches no checklist item.
7. **Pressing Enter at the end of an h1** converts the h1 line itself into `<p>` (contenteditable normalization, log:11818 section); the react7-style flow (type plain lines first, then apply 제목 1) behaves cleanly.
8. **Period picker view-state persists across close/reopen within a mount** (dp-b3 reopened at Aug/Aug after clamp navigation) — the value-anchored re-open behavior applies to committed values (proven by A22/B5 cross-month, log:11765).
9. **1×1px image click target**: `elementFromPoint` probed at fractional offsets before the real click (log:12032).
10. **No Escape tab-reset occurred this session** (the react5-documented agent-browser quirk did not reproduce; Escape checks ran clean on the first attempt).

## B3 pipeline caveat (non-blocking, flagged for the reviewer)

The styles.css comment ("binding vars … emitted per-theme by `podo build`'s components.css") holds **only after** a project imports the badge component document: the CLI's built-in `defaultComponentDocuments` contain just button/input/field (`packages/cli/src/index.ts`), and the tarball ships neither `badge.component.json` nor the `color.palette.*` tokens it references — both live only in `packages/spec/samples/` (the Figma-plugin `podo import` payload shape; `packages/web/src/index.test.ts` documents the intended "badge.component.json → components.css" flow and asserts the runtime CSS never *sets* the vars). Out of the box the badge renders via the identical Figma-value fallbacks (visually indistinguishable), and the override path works regardless — but a consumer cannot get real badge *bindings* emitted without hand-authoring the component JSON. Suggested follow-up: ship the badge document in the CLI defaults or the package, or soften the styles.css comment.

## History appendix (previous rounds — SUPERSEDED, not part of this session's evidence)

- **Round react5** (the previous definitive document): tarball `02fbd90e…` (react5 banner log:2647), 33/33 PASS (24 A-items + 9 B-items incl. disabled-flip, constrained keyboard, reverse period, controlled cross-month, two editors, validator, Field OR, Chip ref). Screenshots h01–h28. Environmental artifacts documented there: Escape tab-resets (twice), Apply-commit next-tick flush, toast eviction animation vs CLI latency, toaster covering h1, editor cross-eval caret loss, B7 message-timer race, code-view toggle title flip.
- **R5 spot-check react6**: tarball `7064859b…` (banner log:5709), 8/8 PASS (validator race with REAL zod, code view A→B→A, named color swatches + keyboard-only table insert, defaultSelected disabled filtering, Field counter external set, searchable focus restore, committed non-step-aligned boundary minute, regression sweep). Shots j01–j10.
- **Audit spot-check react7**: tarball `a80487a3…` (banner log:7972), 9/9 PASS (cross-block color ×2, image click-to-edit popup + delete cleanup, table context-menu edits + focus return, editor explicit 120px height, yearRange/maxDate year select, constrained Home/End, period year clamp, regression sweep). Shots k01–k13. Non-blocking observations recorded there: image edit popup functionally alive but unstyled (no `imageEditPopup` styles-map keys); table context menu can extend below the fold (no flip/scroll).
- **Round react4**: tarball `af9c435a…` (banner log:1), 24/24 PASS. Shots g01–g22.
- **Round react3**: tarball `b7002808…`, 22/22 PASS; report preserved at `reports/react.md.react3-superseded.bak`. Earlier round-2 tarball `ec37fab…` (6-glyph font) superseded.
- Screenshot prefixes by round: g=react4, h=react5, j=react6, k=react7, **m=react8 (this session)**. All prior-round claims are superseded by the m-evidence above.

## Final artifact delta (tarball b691f35c3c93…)

Two findings from the frozen round (session *8) were fixed after it:
1. The default `podo build` now emits the Badge binding vars: the CLI's
   defaultComponentDocuments embeds the spec badge document and the default
   token document gained the `color.palette.*` tokens it references (values ==
   the styles.css fallback hexes). Regression-locked by the CLI build test
   (`--podo-badge-root-background:` / `.podo-badge[data-theme="red"]` /
   `--podo-color-palette-error-5: #FEF1F1` assertions).
2. The Editor's unwired second hidden file input was removed (uploads into it
   were silently inert). Regression-locked by the editor test
   ("renders exactly one file input, wired to the image upload flow").
Repo gate on the final artifact: 23 files / 327 passed | 2 skipped (329).
Micro-verification in the react harness (session microv, logged commands):
fresh install + `podo build --force` → components.css contains 33
`--podo-badge-*` declarations (e.g. `[data-theme="red"] {
--podo-badge-dot-color: var(--podo-color-palette-accent-50); … }`), tokens.css
defines the palette vars; in-browser, the danger Badge's
`--podo-badge-root-background` computes to `#F23B3B` with backgroundColor
rgb(242,59,59) (previously the var computed empty), and each of the 3 mounted
Editors renders exactly one `input[type=file]` (was two each). No other module
changed: only dist/cli and dist/react/editor differ from 634f24ad.

## Closing delta (final tarball 9da8f7ec6667…)

Nine last real bugs from adversarial round 6 were fixed with red→green tests
(repo gate: 23 files / 340 passed | 2 skipped = 342): uncontrolled DatePicker
committed values now render (+`defaultValue` prop; browser-verified in the
react harness: placeholder → "2026 - 07 - 15" after an uncontrolled pick);
Select chip-remove/clear-all keyboard activation no longer swallowed by the
trigger handler; controlled/forced-open Tooltips re-measure on scroll/resize;
re-clicking a selected Editor image keeps a single wrapper; SSR aria-expanded/
aria-controls now track the portal menu's actual presence; native Select
clear-all routes through the stale-event lock; labeled dot Badges are real
accessibility elements (native accessible+image role, hono role="img");
hono Tooltip accepts an id for the spec's aria-describedby contract.
Remaining adversarial-round-6 items are NOT bugs and are catalogued as:
feature backlog (native uncontrolled-state APIs, Field label-press focus,
DatePicker enable/disable-function browser fixtures, Editor toolbar-filter/
resize fixtures, Tooltip position fixtures), design questions (Typography
variant default derived from `as` — consistent react/hono behavior), and
environment-bound evidence (real iOS/Android assistive-technology behavior —
RNW + unit tests are the attainable proxy here).

## Fixture round (react9, tarball 1f56298f9cab674d…)

- **Session `react9`** — closes the "feature backlog: DatePicker enable/disable-function browser fixtures, Editor toolbar-filter/resize fixtures, Tooltip position fixtures" items catalogued in the closing delta above. Banner `=== FIXTURE ROUND session react9 tarball 1f56298f9cab674d ===` at **log:12223**; every browser step ran as `npx agent-browser --session react9 …` through `ab9.sh` (full shell-quoted command lines, entire eval JS included, logged before execution). Date: 2026-07-21.
- **Artifact under test:** `podo-ui-2.1.0.tgz`, `shasum` = `1f56298f9cab674dcc8d7c787189674ad315767c` (quoted log:12225-12226).
- **Clean reinstall proof** (log:12229-12265): pre-reinstall installed version quoted `"version": "2.0.4"` + `REACT9_CANARY` file planted inside `node_modules/podo-ui`; `package.json` dep bumped to `file:../../podo-ui-2.1.0.tgz`; **full** `rm -rf node_modules package-lock.json && npm install` → `found 0 vulnerabilities`; post-install `"version": "2.1.0"` and `REACT9_CANARY: No such file or directory` → the old tree is provably gone.
- **Bit-for-bit freshness proof** (log:12267-12275): 4/4 hash pairs identical between the installed package and a fresh extract of the 1f56298f tarball (`tarball-inspect-react9/`): `dist/react/index.js` = `d83db537…`, `dist/react/datepicker.js` = `14aecf38…`, `dist/react/editor/index.js` = `8f5d8c15…`, `styles.css` = `fa65c26a…` (styles.css is byte-identical to 2.0.4 — the 2.1.0 delta is JS-only). **Build-unique marker** (log:12277-12280): `defaultValue` greps **3** in the installed 2.1.0 `datepicker.js` vs **0** in the react8-round 2.0.4 tarball copy.
- **Regeneration of what the harness consumes** (log:12282-12293): `npx podo build --force` → `[podo:build] Generated 17 files in src/podo.`, fresh mtimes `Jul 21 15:31` quoted next to a UTC timestamp; harness imports `src/podo/{tokens.css,components.css,icons/PodoIcons.css}` (unchanged consumer flow).
- Repo `/Users/tarucy/project/podoui`: READ ONLY (untouched; the fixtures live in `harness/react/src/App.tsx` only). `pnpm check` green this session: **`Test Files  23 passed (23)` / `Tests  349 passed | 2 skipped (351)`** (`check-react9.log`).
- **Harness changes:** `App.tsx` gained sections 19-22 (fixtures below); prop values were written against the repo source contracts read first: `DatePickerProps.enable/disable?: DateCondition[]` (`Date | DateRange | ((date: Date) => boolean)`), `align?: "left" | "right"`, `initialCalendar?: { start?: "now"|"prevMonth"|"nextMonth"|Date … }` (datepicker.tsx:63-112), `EditorProps.toolbar?: ToolbarItem[]` / `resizable?: boolean` (editor/types.ts:18-31), `TooltipProps.position?/ordinal?` (index.tsx:340-352). Note: the task's literal `toolbar={["bold","link"]}` is not type-valid — `ToolbarItem` has no `"bold"`; the bold button belongs to the `"text-style"` group, so the fixture uses the type-correct `toolbar={["text-style","link"]}` (same as the repo unit test editor.test.tsx:459-469).
- Screenshots **n01-n09** in `reports/react-shots/`. Teardown: `close` → `✓ Browser closed`; `kill $(cat server.pid)` → `port5211=down` (end of log).
- **Result: 8 / 8 checks PASS.**

| # | Check | Result | Evidence (quoted from react-commands.log, session react9) |
|---|-------|--------|-----------------------------------------------------------|
| 1 | DatePicker `enable` allowlist — only listed dates clickable; non-listed day disabled; arrow nav skips | **PASS** | log:12315 — grid `2026년 7월`, 31 day cells, `enabledDays:["10","14","21"]` exactly = the allowlist; non-listed day 15 quoted verbatim: `<button type="button" role="gridcell" aria-label="2026년 7월 15일" aria-selected="false" class="podo-dp-calendarCell podo-dp-disabled" data-day="15" tabindex="-1" disabled="">15</button>`; forced `.click()` + synthetic bubbling click on it → readout stays `(none)`, calendar stays open; real click on enabled 14 → readout `2026-07-14`, trigger `2026 - 07 - 14`. Arrow nav (roving tabstop = ["14"] only): ArrowRight from 14 → activeElement `data-day` **21** (15-20 skipped), ArrowLeft → 14, ArrowLeft → **10** (13/12/11 skipped), ArrowLeft again → stays **10** with month unchanged (boundary stop — no enabled day anywhere earlier). Shot n01 |
| 2 | DatePicker function `disable` — weekend callback renders disabled + unpickable | **PASS** | log:12353 — `disable={[d => d.getDay()===0 || d.getDay()===6]}`: `disabledDays:[4,5,11,12,18,19,25,26]` == `weekendDays` (`disabledEqualsWeekends:true`, all July-2026 Sat/Sun); Saturday 18 quoted: `… class="podo-dp-calendarCell podo-dp-disabled" data-day="18" tabindex="-1" disabled=""`; forced+synthetic click on Sat 18 → readout `(none)`; clean click on Friday 17 → readout `2026-07-17`, trigger `2026 - 07 - 17`, calendar closes. Shot n02 |
| 3 | DatePicker `align="right"` — dropdown aligns to the trigger's right edge | **PASS** | log:12398 — dropdown class `podo-dp-dropdown podo-dp-right`, computed `right: 0px`; geometry: input `{left:177, right:341}` vs dropdown `{left:29, right:341}` → `rightEdgesAligned:true`, `leftEdgesAligned:false` (312px calendar hangs leftward). Contrast control in the same viewport: the default-align picker's dropdown is left-pinned (`inputLeft:177 == dropdownLeft:177`, right edge overflows to 489). Shot n03 |
| 4 | DatePicker `initialCalendar={{start:"nextMonth"}}` — opening shows keyword-resolved month | **PASS** | log:12414 — value-less picker (trigger still placeholder `YYYY - MM - DD`), today `2026-7-21` → expected `2026년 8월`; opened grid `aria-label` = `"2026년 8월"` (`matches:true`), nav selects `yearSelect:"2026"`, `monthSelect:"7"` (0-indexed August). Shot n04 |
| 5 | Editor `toolbar={["text-style","link"]}` — exactly those groups + separators; bold/link still function | **PASS** | log:12452 — toolbar snapshot (styles stripped): `groupCount:2`, `buttonTitles:["굵게","기울임","밑줄","취소선","링크"]`, `excludedControlsPresent:[]` (실행 취소/문단 형식/글꼴 색상/정렬/목록/표/이미지/유튜브/구분선/서식 지우기/HTML 코드보기 all absent); separator-per-design: first group `::after {content:"", width:1px, height:20px, background:rgb(228,228,231)}`, last group `::after content:none`. Functions: Range-select `굵게대상` → click 굵게 → readout `<p><b>굵게대상</b> 그리고 링크대상 텍스트</p>`; select `링크대상` → click 링크 (dropdown opens, URL input present, 삽입 disabled while URL empty) → fill + 삽입 → readout gains `<a href="https://example.com/react9" target="_blank" rel="noopener noreferrer">링크대상</a>`. Shot n05 |
| 6 | Editor `resizable` — grip drag changes height within min/max | **PASS** | log:12486 — style contract: container class `podo-ed-editorContainer podo-ed-resizable`, computed `resize: vertical`, inline `{height:220px, minHeight:150px, maxHeight:400px}`, `::-webkit-resizer` styled transparent (the grip IS the native vertical resizer — no separate DOM node by design). **Real grip drag automated** via raw `mouse move/down/up` at the container's bottom-right (right-8, bottom-8): 220px → `computedHeightAfterDragDown80: "300px"` (+80 exact) → 389px → push past ceiling → **`heightClampedAtMax: "400px"`** → ~490px upward drag → **`heightClampedAtMin: "150px"`**. Contrast: editor3 (no `resizable`) computed `resize: none`. Corroborating unit test: editor.test.tsx:517 "applies explicit min/max heights and keeps the height in code view" (`box.style.resize === "vertical"`). Shots n06 (400px max) / n07 (150px min) |
| 7 | Tooltip `position="left"` + ordinal first/second/third — data attrs + geometry vs anchor | **PASS** | log:12556 — three forced-open portal bubbles, each `data-position="left"`, `data-ordinal` correct, `parentElement===document.body`, `position:fixed`; horizontal: bubble right edge == anchor left edge `[417,417]` for all three; vertical tops match the source formula (`TOOLTIP_ARROW_CENTER=12`, index.tsx:1623-1636) to the pixel: first `top 365 == anchorCenter-12`, second `top 431 == anchorCenter-h/2`, third `top 517 == anchorCenter-(h-12)` (`topOK:true` ×3, bubbleH 34, tall 64px anchors distinguish the ordinals). Left arrowhead quoted: 4×16 svg `path d="M0 0v4l4 4-4 4v4z"`; `role="tooltip"`, anchor `aria-describedby == bubble.id` (`_r_11_`). Shot n08 |
| 8 | Regression sweep — reload, errors/console clean | **PASS** | log:12568 — post-reload kitchen sink: `sections:26`, prior-round fixtures intact (`dpBoundary:"2026-07-15 23:59"`, `dpYrTrigger:"1990 - 06 - 15"`, `tableSelected:"3"`, `b4Fixtures:true`, `iconLabeled:true`, `badgeVar:"#F23B3B"`), react9 fixtures intact (`ed4Toolbar:2`, `ed5Resize:"vertical"`, `tt9Bubbles:3`); `errors` → empty; `console` → only Vite debug + React DevTools info lines. Shot n09 |

### Environmental artifacts this round (tool quirks — every affected check PASSed on a clean run; none are app defects)

1. **Escape tab-reset (third occurrence across rounds)** — a `press Escape` reset the tab to `about:blank` (log:12386-12393), same artifact documented in react5/react8. Harness reloaded; all evidence for checks 1-2 had already been captured, and later checks avoid Escape (real outside-clicks close dropdowns instead).
2. **Stale-coordinate click** (log:12362-12369) — the first CLI click on Friday 17 landed outside the just-interacted calendar (outside-click cancel closed it, nothing committed — correct cancel semantics); the clean reopen+click sequence immediately after committed `2026-07-17`.
3. **Covered-trigger click refusal** (log:12416-12446) — the still-open `align="right"` dropdown overlapped the `initialCalendar` picker below it, so agent-browser correctly refused the click until a real outside-click closed it (synthetic `document.body.click()` does NOT close these dropdowns — the outside-click listener needs a real pointer event; a logged observation, not a defect). n04 was retaken after (final capture log:12449-12450; the earlier captures at log:12423/12435 predate the fixture's dropdown opening).
