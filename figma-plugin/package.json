{
  "name": "podo-figma-plugin",
  "version": "0.7.2",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "community:check": "node scripts/check-community-assets.mjs",
    "check": "npm run typecheck && npm run build && npm run community:check",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.109.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0"
  }
}
